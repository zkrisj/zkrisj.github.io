<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zkrisj.gitee.io</id>
    <title>zkrisj</title>
    <updated>2023-01-08T15:39:07.519Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zkrisj.gitee.io"/>
    <link rel="self" href="https://zkrisj.gitee.io/atom.xml"/>
    <subtitle>全栈技术文章记录博客</subtitle>
    <logo>https://zkrisj.gitee.io/images/avatar.png</logo>
    <icon>https://zkrisj.gitee.io/favicon.ico</icon>
    <rights>All rights reserved 2023, zkrisj</rights>
    <entry>
        <title type="html"><![CDATA[兔年让这只🐇发射💣来爆破你的所有坏运]]></title>
        <id>https://zkrisj.gitee.io/post/tu-nian-rang-zhe-ge-tu-zi-fa-she-bao-po-ni-de-pi-yun/</id>
        <link href="https://zkrisj.gitee.io/post/tu-nian-rang-zhe-ge-tu-zi-fa-she-bao-po-ni-de-pi-yun/">
        </link>
        <updated>2023-01-08T10:44:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>Hello，掘友们好！又是一年新春之际，祝福大家兔年快乐！给大家介绍一个有趣的动效（兼容 IE），页面右下角有一只搞怪的兔子，鼠标在页面中悬停时，兔子会跟着做出不同的动作和表情。然后可以在页面中任意位置（离兔子太近不能发射，会伤到兔子😆）点击鼠标，将从兔子眼睛👀里发射炮弹，随之击中的是你的霉 运、压 力、贫 穷、疾 病😮‍💨。</p>
<figure data-type="image" tabindex="1"><img src="https://zkrisj.gitee.io/post-images/1673192334096.gif" alt="" loading="lazy"></figure>
<h2 id="实现">实现</h2>
<ol>
<li>定义一个随机文本块。</li>
</ol>
<pre><code class="language-html">&lt;p id=&quot;p1&quot;&gt;&lt;/p&gt;
</code></pre>
<ol start="2">
<li>定义兔子的构造函数。</li>
</ol>
<pre><code class="language-js">function HoverRabbit() {
  this.explodeImage = new Image();
  this.explodeImage.src = &quot;img/explode.png&quot;;
  for (var i = 1; i &lt;= 6; i++) {
    this.images[i] = new Image();
    this.images[i].src = &quot;img/s&quot; + i + &quot;.png&quot;;
  }
  if (typeof(CanvasGradient) != 'undefined') {
    this.canvas = document.createElement(&quot;canvas&quot;);
    this.canvas.width = screen.width + 100;
    this.canvas.height = screen.height;
    this.canvas.style.position = 'absolute';
    this.canvas.style.left = '0px';
    this.canvas.style.top = '0px';
    this.canvas.style.display = 'none';
    document.body.appendChild(this.canvas);
    this.canvas.style.position = 'fixed';
  }
}
</code></pre>
<ol start="3">
<li>定义兔子原型的属性和方法。</li>
</ol>
<pre><code class="language-js">HoverRabbit.prototype = {
  images: [],
  explodeImage: null,
  eyePositions: [],
  current: 1,
  frame: 1,
  canvas: null,
  interval: null,
  start: function() {
    var me = this;
    this.eyePositions[1] = {
      eye1x: 123,
      eye1y: 47,
      eye2x: 104,
      eye2y: 53
    };
    this.eyePositions[2] = {
      eye1x: 120,
      eye1y: 50,
      eye2x: 101,
      eye2y: 54
    };
    this.eyePositions[3] = {
      eye1x: 119,
      eye1y: 54,
      eye2x: 97,
      eye2y: 56
    };
    this.eyePositions[4] = {
      eye1x: 112,
      eye1y: 61,
      eye2x: 90,
      eye2y: 61
    };
    this.eyePositions[5] = {
      eye1x: 105,
      eye1y: 64,
      eye2x: 85,
      eye2y: 61
    };
    this.eyePositions[6] = {
      eye1x: 98,
      eye1y: 68,
      eye2x: 79,
      eye2y: 56
    };
    document.onmousemove = function(e) {
      me.onmousemove(e);
    }
    if (this.canvas) {
      document.addEventListener(&quot;click&quot;, function(e) {
        me.ondblclick(e);
      });
    }
  },
  onmousemove: function(e) {
    var event = e || window.event;
    var deg = Math.abs(screen.height - event.screenY) / (Math.abs(screen.width - event.screenX) + 1);
    var n = 1;
    if (deg &gt; 2) n = 6;
    else if (deg &gt; 1.4) n = 5;
    else if (deg &gt; 0.7) n = 4;
    else if (deg &gt; 0.45) n = 3;
    else if (deg &gt; 0.2) n = 2;
    this.deg = n;
    if (this.current != n) {
      document.body.style.backgroundImage = &quot;url(&quot; + this.images[n].src + &quot;)&quot;;
      this.current = n;
    }
  },
  drawBomb: function(ctxt, n, x, y) {
    var sx = 64 * (n % 4);
    var sy = 64 * (Math.floor(n / 4));
    ctxt.drawImage(this.explodeImage, sx, sy, 64, 64, x - 32, y - 32, 64, 64);
  },
  ondblclick: function(e) {
    if (this.canvas.style.display != 'none') return;
    var me = this;
    if (e.clientX &gt; window.innerWidth - 200 &amp;&amp; e.clientY &gt; window.innerHeight - 200) return;
    var ctxt = this.canvas.getContext(&quot;2d&quot;);
    this.frame = 1;
    this.interval = setInterval(function(e2) {
      ctxt.clearRect(0, 0, me.canvas.width, me.canvas.height);
      switch (me.frame) {
        case 1:
          ctxt.strokeStyle = 'rgba(247,166,71,1)';
          me.canvas.style.display = 'block';
        case 2:
          if (me.frame == 2) {
            ctxt.strokeStyle = 'rgba(247,166,71,0.5)';
            me.drawBomb(ctxt, 0, e.clientX, e.clientY);
          }
          case 3:
            if (me.frame == 3) {
              ctxt.strokeStyle = 'rgba(247,166,71,0.1)';
              me.drawBomb(ctxt, 1, e.clientX, e.clientY);
            }
            var eye1x = window.innerWidth - me.eyePositions[me.current].eye1x;
            var eye1y = window.innerHeight - me.eyePositions[me.current].eye1y;
            ctxt.lineWidth = 3;
            ctxt.beginPath();
            ctxt.moveTo(eye1x, eye1y);
            ctxt.lineTo(e.clientX, e.clientY);
            ctxt.stroke();
            var eye2x = window.innerWidth - me.eyePositions[me.current].eye2x;
            var eye2y = window.innerHeight - me.eyePositions[me.current].eye2y;
            ctxt.beginPath();
            ctxt.moveTo(eye2x, eye2y);
            ctxt.lineTo(e.clientX, e.clientY);
            p1.textContent = ['霉 运', '压 力', '贫 穷', '疾 病'][Math.floor(Math.random() * 4)];
            p1.style.display = 'block';
            p1.style.transform = 'rotate(' + (-150 + me.deg * 30) + 'deg)';
            p1.style.left = e.clientX - 30 + 'px';
            p1.style.top = e.clientY - 30 + 'px';
            fade(p1);
            ctxt.stroke();
            break;
          case 4:
            me.drawBomb(ctxt, 2, e.clientX, e.clientY);
            break;
          case 14:
            me.canvas.style.display = 'none';
            window.clearInterval(me.interval);
            break;
          default:
            me.drawBomb(ctxt, me.frame - 2, e.clientX, e.clientY);
      }
      me.frame++;
    }, 50);
  }
};
</code></pre>
<p>各个属性和方法说明：</p>
<ul>
<li>images - 兔子不同的动作的图片数组。</li>
<li>explodeImage - 炮弹图片元素。</li>
<li>eyePositions - 兔子眼睛位置的数组。</li>
<li>current - 整型数字，当前动作的指针。</li>
<li>frame - 整型数字，发射炮弹动画的帧数指针。</li>
<li>canvas - 画布元素。</li>
<li>interval - 发射炮弹动画时间间隔定时器的 interval ID。</li>
<li>start - 启动页面交互的方法，在这里定义了兔子眼睛位置的数组数据，绑定页面鼠标移动、点击事件。</li>
<li>onmousemove - 定义页面鼠标移动的实现方法。</li>
<li>ondblclick - 定义页面鼠标点击的实现方法。</li>
<li>drawBomb - 定义绘制和更新炮弹动画的方法。</li>
</ul>
<ol start="4">
<li>定义文字淡出的动画。</li>
</ol>
<pre><code class="language-js">function fade(e) {
  var s = e.style;
  s.opacity = 1;
  (function hide() {
    (s.opacity -= .01) &lt; 0 ? s.display = &quot;none&quot; : requestAnimationFrame(hide);
  })();
}
</code></pre>
<ol start="5">
<li>创建兔子对象，调用启动交互方法。</li>
</ol>
<pre><code class="language-js">var s = new HoverRabbit();
s.start();
</code></pre>
<h2 id="码上掘金">码上掘金</h2>
<iframe src="https://code.juejin.cn/pen/7186181146755989562"></iframe>]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用 ECharts 制作掘金个人信息报表]]></title>
        <id>https://zkrisj.gitee.io/post/shi-yong-echarts-zhi-zuo-jue-jin-ge-ren-xin-xi-bao-biao/</id>
        <link href="https://zkrisj.gitee.io/post/shi-yong-echarts-zhi-zuo-jue-jin-ge-ren-xin-xi-bao-biao/">
        </link>
        <updated>2023-01-06T12:37:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>码上掘金迎新年🎉活动开始好久了，大佬们也已经发了各种<a href="https://juejin.cn/challenge/2/result">总结报告</a>。但是我在码上掘金上面请求 api.juejin.cn 接口用户数据时报错 &quot;name&quot;:&quot;TypeError&quot;,&quot;message&quot;:&quot;Failed to fetch&quot;，通过打印 console.log(location.href) 显示为 code.devrank.cn，这应该是跨域的问题。</p>
<p>所以，有些总结报告不能正常显示应该都是类似的问题。昨天终于发现<a href="https://juejin.cn/user/2682464103060541">茶无味的一天</a>的作品<a href="https://code.juejin.cn/pen/7178839138609659959">2022年终总结互动网页</a>，可以正常显示掘金接口的数据。</p>
<figure data-type="image" tabindex="1"><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/3d235c8144ed4a889fff7922f3250f39~tplv-k3u1fbpfcp-watermark.image?" alt="image.png" loading="lazy"></figure>
<p>给大佬留言得到回复是请求的跨域请求的代理网站，因为发现还没有人做关于 ECharts 的报表，所以征得同意可以调用接口后，做了这个 <a href="https://code.juejin.cn/pen/7184730128830693376">ECharts 报表</a>。</p>
<h2 id="echarts-报表">ECharts 报表</h2>
<p>是百度的一个开源的数据可视化工具，一个纯 Javascript 的图表库，能够在 PC 端和移动设备上流畅运行，兼容当前绝大部分浏览器。</p>
<h3 id="引入-echarts">引入 ECharts</h3>
<p>虽然我们大部分使用 ECharts 是在 node 项目中，但为了简单，这里通过 CDN 链接的方式引入。</p>
<pre><code>https://cdn.staticfile.org/echarts/4.7.0/echarts.min.js
</code></pre>
<h3 id="准备容器">准备容器</h3>
<p>使用 Echarts 绘制图表前需要先有一个有宽高的 DOM 容器，代码如下：</p>
<pre><code class="language-html">&lt;div id=&quot;main&quot; style=&quot;width: 600px; height: 400px;&quot;&gt;&lt;/div&gt;
&lt;div id=&quot;main2&quot; style=&quot;width: 600px; height: 400px;&quot;&gt;&lt;/div&gt;
&lt;div id=&quot;main3&quot; style=&quot;width: 100%;height: 400px;&quot;&gt;&lt;/div&gt;
</code></pre>
<p>以上定义了三个容器，一个饼图用来显示掘金账号信息，一个饼图用来显示用户的创作信息，一个柱图用来显示用户阅读量前十文章的阅读量、点赞、收藏、评论数量。</p>
<h3 id="初始化容器">初始化容器</h3>
<p>然后就可以通过 echarts.init 方法初始化 echarts 实例。</p>
<pre><code class="language-js">const myChart = echarts.init(document.getElementById('main'));
const myChart2 = echarts.init(document.getElementById('main2'));
const myChart3 = echarts.init(document.getElementById('main3'));
</code></pre>
<h3 id="开启-loading-效果">开启 loading 效果</h3>
<p>如果数据获取需要一定时间，我们可以使用 ECharts 的 loading 效果。</p>
<pre><code class="language-js">myChart.showLoading();
myChart2.showLoading();
myChart3.showLoading();
</code></pre>
<h3 id="获取掘金-id">获取掘金 ID</h3>
<p>通过 prompt 函数获取输入的掘金 ID，这里设置了默认值为我自己的。</p>
<pre><code class="language-js">const id = prompt('请输入您的掘金 ID', '2208296065832264');
</code></pre>
<h3 id="获取数据">获取数据</h3>
<p>通过浏览器的 fetch 方法来获取代理网站的掘金网站接口数据。</p>
<pre><code class="language-js">fetch('https://juejin.palxp.com/user?id=' + id).then(res =&gt; res.json())
</code></pre>
<p>在请求数据成功时，隐藏 loading 效果。</p>
<pre><code class="language-js">myChart.hideLoading();
myChart2.hideLoading();
myChart3.hideLoading();
const { list, user } = res.result;
const collect_count = list.map(x =&gt; +x.collect_count).reduce((a, b) =&gt; a + b, 0);
const { digg, view, fans, follow, power, name } = user;
</code></pre>
<p>各个字段的说明：</p>
<ul>
<li>list - 文章列表。</li>
<li>user - 用户信息。</li>
<li>collect_count - 由于没有收藏总数这个字段，所以根据每篇文章的收藏数叠加得到收藏总数。</li>
<li>digg - 文章点赞总数。</li>
<li>view - 文章浏览总数。</li>
<li>fans - 用户关注者数量。</li>
<li>follow - 用户关注数量。</li>
<li>power - 用户掘力值。</li>
<li>name - 掘金用户名。</li>
</ul>
<p>对文章列表根据阅读量排序，截取阅读量前十的文章。</p>
<pre><code class="language-js">const ten = list.sort((a, b) =&gt; b.view_count - a.view_count).slice(0, 10);
</code></pre>
<h3 id="配置信息">配置信息</h3>
<p>使用 json 格式来设置配置信息。</p>
<pre><code class="language-js">const option = {
  title: {
    text: name + ' 账号信息',
    x: 'center'
  },
  tooltip: {
    formatter: &quot;{a} &lt;br/&gt;{b} : {c} ({d}%)&quot;
  },
  series: [{
    name: name + ' 账号信息',
    type: 'pie',
    radius: '55%',
    data: [{
      name: '关注者',
      value: fans
    }, {
      name: '关注',
      value: follow
    }, {
      name: '掘力值',
      value: power
    }]
  }]
};
const option2 = {
  title: {
    text: name + ' 创作信息',
    x: 'center'
  },
  tooltip: {
    formatter: &quot;{a} &lt;br/&gt;{b} : {c} ({d}%)&quot;
  },
  series: [{
    name: name + ' 创作信息',
    type: 'pie',
    radius: '55%',
    data: [{
      name: '获得点赞',
      value: digg
    }, {
      name: '获得收藏',
      value: collect_count
    }, {
      name: '文章被阅读',
      value: view
    }]
  }]
};
const option3 = {
  title: {
    text: '阅读量前十文章',
  },
  tooltip: {},
  legend: {
    right: 0,
    data: ['阅读量', '点赞', '收藏', '评论'],
    selected: {
      '阅读量': false,
    }
  },
  grid: {
    x: isMobile ? 150 : 250,
  },
  xAxis: {},
  yAxis: {
    data: ten.map(v =&gt; v.title),
    axisLabel: {
      textStyle: {
        fontSize: isMobile ? 5 : 9
      }
    }
  },
  series: [{
    name: '阅读量',
    type: 'bar',
    data: ten.map(v =&gt; v.view_count)
  }, {
    name: '点赞',
    type: 'bar',
    data: ten.map(v =&gt; v.digg_count)
  }, {
    name: '收藏',
    type: 'bar',
    data: ten.map(v =&gt; v.collect_count)
  }, {
    name: '评论',
    type: 'bar',
    data: ten.map(v =&gt; v.comment_count)
  }]
};
myChart.setOption(option);
myChart2.setOption(option2);
myChart3.setOption(option3);
</code></pre>
<p>各个字段的说明：</p>
<ul>
<li>title - 图表标题。</li>
<li>tooltip - 鼠标悬停时提示框信息。</li>
<li>series - 系列列表。
<ul>
<li>name - 系列名称。</li>
<li>type - 每个系列通过 type 决定自己的图表类型。</li>
<li>radius - 饼图的半径。</li>
<li>data - 系列的数据，name 和 value 的键值对。</li>
</ul>
</li>
</ul>
<p>由于移动端比 PC 端屏幕宽度小，所以判断一下设备，移动端设置较小的字体和坐标轴标签宽度。</p>
<pre><code class="language-js">const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
</code></pre>
<p>由于柱图中阅读量和其他分类的数量差距比较大，所以默认隐藏阅读量数据显示，可以点击右侧类别图标切换显示。</p>
<figure data-type="image" tabindex="2"><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/57dedf31ba4a424ba29d510fefe772ea~tplv-k3u1fbpfcp-watermark.image?" alt="echarts.gif" loading="lazy"></figure>
<h3 id="设置循环动画">设置循环动画</h3>
<p>有时候我们会需要在程序里调用方法并触发图表的行为，比如显示 tooltip。ECharts 通过 dispatchAction({ type: '' }) 来触发图表行为，统一管理了所有动作，也可以根据需要去记录用户的行为路径。</p>
<pre><code class="language-js">function loop(myChart, option) {
  let currentIndex = -1;
  setInterval(function() {
    const { length } = option.series[0].data;
    myChart.dispatchAction({
      type: 'downplay',
      seriesIndex: 0,
      dataIndex: currentIndex
    });
    currentIndex = (currentIndex + 1) % length;
    myChart.dispatchAction({
      type: 'highlight',
      seriesIndex: 0,
      dataIndex: currentIndex
    });
    myChart.dispatchAction({
      type: 'showTip',
      seriesIndex: 0,
      dataIndex: currentIndex
    });
  }, 1000);
}
</code></pre>
<p>这里我们通过 setInterval 函数循环触发三个 Action。</p>
<ul>
<li>downplay - 取消高亮指定的数据图形。</li>
<li>highlight - 高亮指定的数据图形。</li>
<li>showTip - 有下面两种使用方式。
<ul>
<li>指定在相对容器的位置处显示提示框。</li>
</ul>
<pre><code class="language-js">dispatchAction({
    type: 'showTip',
    // 屏幕上的 x 坐标
    x: number,
    // 屏幕上的 y 坐标
    y: number,
    // 本次显示 tooltip 的位置。只在本次 action 中生效。
    // 缺省则使用 option 中定义的 tooltip 位置。
    position: Array.&lt;number&gt;|string|Function
})
</code></pre>
<ul>
<li>根据 tooltip 的配置项显示提示框。</li>
</ul>
<pre><code class="language-js">dispatchAction({
    type: 'showTip',
    // 系列的 index，在 tooltip 的 trigger 为 axis 的时候可选。
    seriesIndex?: number,
    // 数据的 index，如果不指定也可以通过 name 属性根据名称指定数据
    dataIndex?: number,
    // 可选，数据名称，在有 dataIndex 的时候忽略
    name?: string,
    // 本次显示 tooltip 的位置。只在本次 action 中生效。
    // 缺省则使用 option 中定义的 tooltip 位置。
    position: Array.&lt;number&gt;|string|Function,
})
</code></pre>
</li>
</ul>
<h2 id="码上掘金">码上掘金</h2>
<iframe src="https://code.juejin.cn/pen/7184730128830693376"></iframe>]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[前端本地存储数据库 IndexedDB 存储文件]]></title>
        <id>https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-cun-chu-wen-jian/</id>
        <link href="https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-cun-chu-wen-jian/">
        </link>
        <updated>2022-12-16T14:49:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<figure data-type="image" tabindex="1"><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ff12ea5c5c484009a4776744a294b685~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221209222347.png" loading="lazy"></figure>
<p>IndexedDB 是一种浏览器底层 API，目前各浏览器都已支持，兼容性很好。</p>
<h2 id="特点">特点</h2>
<ol>
<li>IndexedDB 内部采用对象仓库（object store）存放键值对数据。可以存储结构化克隆算法支持的任何对象。每一个数据记录都有对应的主键，主键是独一无二的，并且不能有重复。</li>
<li>使用索引实现对数据的高性能搜索。</li>
<li>IndexedDB API 大部分都是异步的。不会通过返回值提供数据，而是要传递一个回调函数来获取返回值。</li>
<li>同大多数 Web 存储解决方案一样，IndexedDB 遵循同源策略。即可以访问相同域内存储的数据，但无法跨不同域访问数据。</li>
<li>可以存储文件、二进制对象。</li>
<li>在 Web Worker 中可用。</li>
<li>IndexedDB 属于 NoSQL 和事务型面向对象数据库系统。对数据库的所有操作，都要通过事务完成。</li>
</ol>
<p>IndexedDB 允许存储和检索用键索引的对象，它可以存储结构化克隆算法支持的任何对象。</p>
<h2 id="结构化克隆">结构化克隆</h2>
<p>结构化克隆是由 HTML5 规范定义的用于复制复杂 JavaScript 对象的算法，例如 <code>structuredClone()</code> 函数的调用，<code>Workers</code> 的 <code>postMessage()</code> 方法或使用 <code>IndexedDB</code> 存储对象时在内部的调用。结构化克隆算法使 <code>postMessage()</code> 在 <code>Worker</code> 和主线程之间不再仅仅传输字符串类型，还可以传输包括 <code>File</code>, <code>Blob</code>, <code>ArrayBuffer</code>, <code>JSON</code> 对象类型。支持的类型：</p>
<ul>
<li><code>symbols</code> 除外的所有原始类型。</li>
<li><code>Boolean</code> <code>String</code> 原始类型包装对象。</li>
<li><code>Array</code> <code>Map</code> <code>Set</code> <code>Date</code> <code>RegExp</code> 内置对象。</li>
<li><code>Blob</code> <code>File</code> <code>FileList</code> 文件类型对象。</li>
<li><code>ArrayBuffer</code> <code>TypedArray</code> <code>DataView</code> <code>Buffer</code> 类型对象。</li>
<li><code>ImageBitmap</code> <code>ImageData</code> 图形对象。</li>
<li><code>Object</code> 仅包括普通对象，例如对象字面量。</li>
<li><code>Error</code>, <code>EvalError</code>, <code>RangeError</code>, <code>ReferenceError</code>, <code>SyntaxError</code>, <code>TypeError</code>, <code>URIError</code> 错误类型，其他类型错误 <code>name</code> 属性会被转成 <code>Error</code>，<code>message</code> 属性会丢失。</li>
</ul>
<h2 id="indexeddb-存储文件">IndexedDB 存储文件</h2>
<p>下面我们通过在页面中定义一个文件上传按钮，选择图片文件后，会将文件保存到 IndexedDB 数据库中，当我们刷新页面，或关闭页面后，再次进入，会发现图片文件可以从 IndexedDB 数据库中读取，然后在页面中显示。</p>
<pre><code class="language-html">&lt;body&gt;
  &lt;label for=&quot;fileImg&quot;&gt;上传图片&lt;/label&gt;
  &lt;input type=&quot;file&quot; id=&quot;fileImg&quot; accept=&quot;image/*&quot; hidden&gt;
  &lt;p id=&quot;result&quot;&gt;&lt;/p&gt;
&lt;/body&gt;
&lt;script&gt;
  let db;
  const request = indexedDB.open('the_file', 1);
  request.onerror = console.log;
  request.onsuccess = function(ev) {
    console.log('打开');
    db = request.result;
    db.transaction(db.objectStoreNames).objectStore('files').openCursor().onsuccess = function(event) {
      const cursor = event.target.result;
      if (cursor) {
        console.log(cursor.value);
        const img = new Image();
        img.src = URL.createObjectURL(cursor.value.file);
        result.append(img);
        cursor.continue();
      }
    }
  };
  request.onupgradeneeded = function(ev) {
    console.log('升级');
    db = request.result;
    if (!db.objectStoreNames.contains('files')) {
      const table = db.createObjectStore('files', { keyPath: 'id', autoIncrement: true });
      table.createIndex('file', 'file', { unique: false });
    }
  };
  fileImg.onchange = e =&gt; {
    const file = fileImg.files[0];
    const request = db.transaction(['files'], 'readwrite').objectStore('files').add({ file });
    request.onsuccess = function(event) {
      console.log('数据写入成功');
      const img = new Image();
      img.src = URL.createObjectURL(file);
      result.append(img);
    };
    request.onerror = function(event) { console.log('数据写入失败'); }
  };
&lt;/script&gt;
&lt;style&gt;
  label {
    display: inline-block;
    padding: 1em;
    border-radius: .5em;
    background: red;
    color: white;
    font-family: Arial, Helvetica, sans-serif
  }
&lt;/style&gt;
</code></pre>
<h2 id="码上掘金">码上掘金</h2>
<iframe src="https://code.juejin.cn/pen/7177758045026385980"></iframe>
<h2 id="历史相关文章">历史相关文章</h2>
<ul>
<li><a href="https://juejin.cn/post/7176211543330144315">前端本地存储数据库 IndexedDB 的增删改查</a></li>
<li><a href="https://juejin.cn/post/7176556381376348215">前端本地存储数据库 IndexedDB 和 Web SQL、Web Storage 的对比</a></li>
<li><a href="https://juejin.cn/post/7177069487349301306">前端本地存储数据库 IndexedDB 的范围查询和对结果计数</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[前端本地存储数据库 IndexedDB 的范围查询和对结果计数]]></title>
        <id>https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-de-fan-wei-sou-suo-he-ji-shu/</id>
        <link href="https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-de-fan-wei-sou-suo-he-ji-shu/">
        </link>
        <updated>2022-12-14T18:32:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<figure data-type="image" tabindex="1"><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ff12ea5c5c484009a4776744a294b685~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221209222347.png" loading="lazy"></figure>
<p>IndexedDB 是一种浏览器底层 API，目前各浏览器都已支持，兼容性很好。</p>
<ol>
<li>IndexedDB 内部采用对象仓库（object store）存放键值对数据。可以存储结构化克隆算法支持的任何对象。每一个数据记录都有对应的主键，主键是独一无二的，并且不能有重复。</li>
<li>使用索引实现对数据的高性能搜索。</li>
<li>IndexedDB API 大部分都是异步的。不会通过返回值提供数据，而是要传递一个回调函数来获取返回值。</li>
<li>同大多数 Web 存储解决方案一样，IndexedDB 遵循同源策略。即可以访问相同域内存储的数据，但无法跨不同域访问数据。</li>
<li>可以存储文件/二进制对象。</li>
<li>在 Web Worker 中可用。</li>
<li>IndexedDB 属于 NoSQL 和事务型面向对象数据库系统。对数据库的所有操作，都要通过事务完成。</li>
</ol>
<p>后端开发者都知道，SQL 语句中有 BETWEEN 操作符和 COUNT() 函数可以用来对匹配的记录进行过滤和计数。IndexedDB 也提供了 IDBKeyRange 接口和 IDBObjectStore.count() 方法可以实现相同的目的。</p>
<pre><code class="language-sql">SELECT COUNT(*) FROM Persons WHERE LastName BETWEEN 'Adams' AND 'Carter';
</code></pre>
<h2 id="范围搜索">范围搜索</h2>
<p>键集合 IDBKeyRange 对象通过使用一个键或某个范围的键从 IDBObjectStore 和 IDBIndex 对象中检索记录，并可以使用下限和上限来限制范围。下表给出了检索特定范围内的方法：</p>
<table>
<thead>
<tr>
<th>特定范围</th>
<th>方法</th>
</tr>
</thead>
<tbody>
<tr>
<td>All keys ≥ x</td>
<td>IDBKeyRange.lowerBound (x)</td>
</tr>
<tr>
<td>All keys &gt; x</td>
<td>IDBKeyRange.lowerBound (x, true)</td>
</tr>
<tr>
<td>All keys ≤ y</td>
<td>IDBKeyRange.upperBound (y)</td>
</tr>
<tr>
<td>All keys &lt; y</td>
<td>IDBKeyRange.upperBound (y, true)</td>
</tr>
<tr>
<td>All keys ≥ x &amp;&amp; ≤ y</td>
<td>IDBKeyRange.bound (x, y)</td>
</tr>
<tr>
<td>All keys &gt; x &amp;&amp;&lt; y</td>
<td>IDBKeyRange.bound (x, y, true, true)</td>
</tr>
<tr>
<td>All keys &gt; x &amp;&amp; ≤ y</td>
<td>IDBKeyRange.bound (x, y, true, false)</td>
</tr>
<tr>
<td>All keys ≥ x &amp;&amp;&lt; y</td>
<td>IDBKeyRange.bound (x, y, false, true)</td>
</tr>
<tr>
<td>The key = z</td>
<td>IDBKeyRange.only (z)</td>
</tr>
</tbody>
</table>
<p>bound()、only()、lowerBound()、upperBound() 方法即是方法名字面意思：“范围内”、“仅仅是”、“小于某值、“大于某值”。方法最后还支持两个布尔值参数，布尔值参数为 true 的时候表示不含范围边界。</p>
<h2 id="结果计数">结果计数</h2>
<p>IDBObjectStore 接口的 count(query) 方法返回一个 IDBRequest 对象，并在单独的线程中返回与提供的键或匹配的记录总数。其中 query 参数可以是键属性或 IDBKeyRange 对象。如果未提供参数，则返回存储中的记录总数。</p>
<h2 id="使用">使用</h2>
<p>下面我们在 IndexedDB 存储中插入一系列数据，然后通过 IDBKeyRange 对象对结果进行范围搜索，并使用 IDBObjectStore 接口的 count(query) 方法对结果进行计数。</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;body&gt;
  &lt;ul&gt;&lt;/ul&gt;
  &lt;form&gt;
    &lt;div&gt;
      &lt;div&gt;
        &lt;label for=&quot;none&quot;&gt;不筛选&lt;/label&gt;
        &lt;input type=&quot;radio&quot; name=&quot;value&quot; value=&quot;none&quot; id=&quot;none&quot; checked /&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;div&gt;
        &lt;label for=&quot;only&quot;&gt;单个值筛选&lt;/label&gt;
        &lt;input type=&quot;radio&quot; name=&quot;value&quot; value=&quot;only&quot; id=&quot;only&quot; /&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;label for=&quot;onlytext&quot;&gt;值&lt;/label&gt;
        &lt;input type=&quot;text&quot; name=&quot;onlytext&quot; id=&quot;onlytext&quot; /&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;div&gt;
        &lt;label for=&quot;range&quot;&gt;筛选范围内的值&lt;/label&gt;
        &lt;input type=&quot;radio&quot; name=&quot;value&quot; value=&quot;range&quot; id=&quot;range&quot; /&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;label for=&quot;rangelowertext&quot;&gt;范围下限&lt;/label&gt;
        &lt;input type=&quot;text&quot; name=&quot;rangelowertext&quot; id=&quot;rangelowertext&quot; /&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;label for=&quot;rangeuppertext&quot;&gt;范围上限&lt;/label&gt;
        &lt;input type=&quot;text&quot; name=&quot;rangeuppertext&quot; id=&quot;rangeuppertext&quot; /&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;div&gt;
        &lt;label for=&quot;lower&quot;&gt;筛选不低于下限的值&lt;/label&gt;
        &lt;input type=&quot;radio&quot; name=&quot;value&quot; value=&quot;lower&quot; id=&quot;lower&quot; /&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;label for=&quot;lowerboundtext&quot;&gt;下限&lt;/label&gt;
        &lt;input type=&quot;text&quot; name=&quot;lowerboundtext&quot; id=&quot;lowerboundtext&quot; /&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;div&gt;
        &lt;label for=&quot;upper&quot;&gt;筛选不高于上限的值&lt;/label&gt;
        &lt;input type=&quot;radio&quot; name=&quot;value&quot; value=&quot;upper&quot; id=&quot;upper&quot; /&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;label for=&quot;upperboundtext&quot;&gt;上限&lt;/label&gt;
        &lt;input type=&quot;text&quot; name=&quot;upperboundtext&quot; id=&quot;upperboundtext&quot; /&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;div&gt;
        &lt;input type=&quot;radio&quot; name=&quot;filterIndex&quot; value=&quot;fThing&quot; id=&quot;thing&quot; checked /&gt;
        &lt;label for=&quot;thing&quot;&gt;按名称筛选和排序&lt;/label&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;input type=&quot;radio&quot; name=&quot;filterIndex&quot; value=&quot;fRating&quot; id=&quot;rating&quot; /&gt;
        &lt;label for=&quot;rating&quot;&gt;按评分筛选和排序&lt;/label&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;input class=&quot;run&quot; type=&quot;submit&quot; value=&quot;查询&quot;&gt;
  &lt;/form&gt;
&lt;/body&gt;
&lt;script&gt;
  var db;
  var things = [
    { fThing: &quot;Drum kit&quot;, fRating: 10 },
    { fThing: &quot;Family&quot;, fRating: 10 },
    { fThing: &quot;Batman&quot;, fRating: 9 },
    { fThing: &quot;Brass eye&quot;, fRating: 9 },
    { fThing: &quot;The web&quot;, fRating: 9 },
    { fThing: &quot;Mozilla&quot;, fRating: 9 },
    { fThing: &quot;Firefox OS&quot;, fRating: 9 },
    { fThing: &quot;Curry&quot;, fRating: 9 },
    { fThing: &quot;Paneer cheese&quot;, fRating: 8 },
    { fThing: &quot;Mexican food&quot;, fRating: 8 },
    { fThing: &quot;Chocolate&quot;, fRating: 7 },
    { fThing: &quot;Heavy metal&quot;, fRating: 10 },
    { fThing: &quot;Monty Python&quot;, fRating: 8 },
    { fThing: &quot;Aphex Twin&quot;, fRating: 8 },
    { fThing: &quot;Gaming&quot;, fRating: 7 },
    { fThing: &quot;Frank Zappa&quot;, fRating: 9 },
    { fThing: &quot;Open minds&quot;, fRating: 10 },
    { fThing: &quot;Hugs&quot;, fRating: 9 },
    { fThing: &quot;Ale&quot;, fRating: 9 },
    { fThing: &quot;Christmas&quot;, fRating: 8 },
    ];
  var list = document.querySelector(&quot;ul&quot;);
  var form = document.querySelector(&quot;form&quot;);
  var thing = document.querySelector(&quot;#thing&quot;);
  var rating = document.querySelector(&quot;#rating&quot;);
  var button = document.querySelector(&quot;button&quot;);
  var onlyText = document.querySelector(&quot;#onlytext&quot;);
  var rangeLowerText = document.querySelector(&quot;#rangelowertext&quot;);
  var rangeUpperText = document.querySelector(&quot;#rangeuppertext&quot;);
  var lowerBoundText = document.querySelector(&quot;#lowerboundtext&quot;);
  var upperBoundText = document.querySelector(&quot;#upperboundtext&quot;);
  window.onload = function() {
    rating.onchange = e =&gt; {
      document.querySelectorAll('input[type=text]').forEach(v =&gt; {
        v.setAttribute('pattern', '[0-9]');
        v.title = '请输入数字！';
      });
    };
    thing.onchange = e =&gt; {
      document.querySelectorAll('input[type=text]').forEach(v =&gt; {
        v.setAttribute('pattern', '[A-Z]{1}');
        v.title = '请输入单个大写字母！';
      });
    };
    form.onsubmit = e =&gt; {
      e.preventDefault();
      displayData();
    };
    window.indexedDB = window.indexedDB || window.mozIndexedDB || window.webkitIndexedDB || window.msIndexedDB;
    window.IDBTransaction = window.IDBTransaction || window.webkitIDBTransaction || window.msIDBTransaction;
    window.IDBKeyRange = window.IDBKeyRange || window.webkitIDBKeyRange || window.msIDBKeyRange;
    var DBOpenRequest = window.indexedDB.open(&quot;fThings&quot;, 1);
    DBOpenRequest.onsuccess = function(event) {
      db = DBOpenRequest.result;
      populateData();
    };
    DBOpenRequest.onupgradeneeded = function(event) {
      var db = event.target.result;
      db.onerror = function(event) {
        note.innerHTML += &quot;&lt;li&gt;Error loading database.&lt;/li&gt;&quot;;
      };
      var objectStore = db.createObjectStore(&quot;fThings&quot;, { keyPath: &quot;fThing&quot; });
      objectStore.createIndex(&quot;fRating&quot;, &quot;fRating&quot;, { unique: false });
    };

    function populateData() {
      var transaction = db.transaction([&quot;fThings&quot;], &quot;readwrite&quot;);
      var objectStore = transaction.objectStore(&quot;fThings&quot;);
      for (i = 0; i &lt; things.length; i++) {
        var request = objectStore.put(things[i]);
      }
      transaction.oncomplete = function() {
        displayData();
      };
    }
    var keyRangeValue = null;

    function displayData() {
      checkedValue = document.querySelector('input[name=&quot;value&quot;]:checked').value;
      var transaction = db.transaction([&quot;fThings&quot;], &quot;readonly&quot;);
      var objectStore = transaction.objectStore(&quot;fThings&quot;);
      var filterIndex = document.querySelector('input[name=&quot;filterIndex&quot;]:checked').value;
      if (filterIndex == &quot;fThing&quot;) {
        if (checkedValue == &quot;none&quot;) {
          keyRangeValue = null;
        } else if (checkedValue == &quot;only&quot;) {
          keyRangeValue = IDBKeyRange.only(onlyText.value);
        } else if (checkedValue == &quot;range&quot;) {
          keyRangeValue = IDBKeyRange.bound(rangeLowerText.value, rangeUpperText.value, false, false);
        } else if (checkedValue == &quot;lower&quot;) {
          keyRangeValue = IDBKeyRange.lowerBound(lowerBoundText.value);
        } else if (checkedValue == &quot;upper&quot;) {
          keyRangeValue = IDBKeyRange.upperBound(upperBoundText.value);
        }
      } else {
        //遍历 fRating 索引而不是对象存储：
        objectStore = objectStore.index(&quot;fRating&quot;);
        if (checkedValue == &quot;none&quot;) {
          keyRangeValue = null;
        } else if (checkedValue == &quot;only&quot;) {
          keyRangeValue = IDBKeyRange.only(parseFloat(onlyText.value));
        } else if (checkedValue == &quot;range&quot;) {
          keyRangeValue = IDBKeyRange.bound(parseFloat(rangeLowerText.value), parseFloat(rangeUpperText.value), false, false);
        } else if (checkedValue == &quot;lower&quot;) {
          keyRangeValue = IDBKeyRange.lowerBound(parseFloat(lowerBoundText.value));
        } else if (checkedValue == &quot;upper&quot;) {
          console.log(upperBoundText.value);
          keyRangeValue = IDBKeyRange.upperBound(parseFloat(upperBoundText.value));
        }
      }
      if (keyRangeValue != null) {
        const { lower, upper, lowerOpen, upperOpen } = keyRangeValue;
        console.log(`范围的下限: ${lower},下限值是否包含在键范围内: ${lowerOpen}\n
          范围的上限: ${upper}, 上限值是否包含在键范围内: ${upperOpen}`);
      }
      list.innerHTML = &quot;&quot;;
      objectStore.openCursor(keyRangeValue).onsuccess = function(event) {
        var cursor = event.target.result;
        if (cursor) {
          var listItem = document.createElement(&quot;li&quot;);
          listItem.innerHTML = &quot;&lt;strong&gt;&quot; + cursor.value.fThing + &quot;&lt;/strong&gt;, &quot; + cursor.value.fRating;
          list.appendChild(listItem);
          cursor.continue();
        }
      };
      var countRequest = objectStore.count(keyRangeValue);
      countRequest.onsuccess = function() {
        var listItem = document.createElement(&quot;li&quot;);
        listItem.innerHTML = &quot;&lt;strong&gt;总计&lt;/strong&gt;: &quot; + countRequest.result;
        list.insertAdjacentElement('afterbegin', listItem);
      };
    };
  };
&lt;/script&gt;
&lt;style&gt;
  ul {
    list-style-type: none;
    flex: 100px;
    margin-right: 1em;
    padding: 2rem 1rem;
    text-align: center;
    background: white linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.2));
    box-shadow: 3px 3px 10px black;
  }
  li {
    padding-bottom: 1rem;
  }
  body {
    display: flex;
  }
  form {
    flex: 5;
    margin-top: 1rem;
  }
  form&gt;div {
    margin-bottom: 1rem;
    padding: 1rem 1rem 2rem;
    background: white linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.2));
    box-shadow: 3px 3px 10px black;
  }
  form div div {
    clear: both;
    margin-top: 1rem;
  }
  form div label {
    width: 55%;
    clear: both;
  }
  form div input {
    float: right;
  }
  form div input[type=&quot;text&quot;] {
    width: 30%;
  }
  form div input[type=&quot;radio&quot;] {
    margin-right: 10px;
  }
  button {
    display: block;
    width: 30%;
    margin: 0 auto;
    line-height: 1.5;
    box-shadow: 1px 1px 2px black;
  }
  :invalid {
    outline-color: red;
  }
&lt;/style&gt;
</code></pre>
<h2 id="马上掘金">马上掘金</h2>
<iframe src="https://code.juejin.cn/pen/7177060500306395140"></iframe>
<h2 id="其他">其他</h2>
<ul>
<li><a href="https://juejin.cn/post/7176211543330144315">前端本地存储数据库 IndexedDB 的增删改查</a></li>
<li><a href="https://juejin.cn/post/7176556381376348215">前端本地存储数据库 IndexedDB 和 Web SQL、Web Storage 的对比</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[前端本地存储数据库 IndexedDB 和 Web SQL、Web Storage 的对比]]></title>
        <id>https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-he-web-sqlweb-storage-de-dui-bi/</id>
        <link href="https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-he-web-sqlweb-storage-de-dui-bi/">
        </link>
        <updated>2022-12-13T09:00:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<p>将大量数据储存在客户端，这样可以减少从服务器获取数据的压力，提升用户体验。现有的浏览器数据储存方案，都不适合储存大量数据：Cookie 的大小不超过4KB，且每次请求都会发送回服务器；Storage 存储在 5MB 左右，各浏览器不同，而且不提供搜索功能，也不能建立自定义的索引。所以，需要一种新的解决方案在客户端存储数据。</p>
<figure data-type="image" tabindex="1"><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/da43f47ea94346e8b8d8cba6ccf977d1~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221203165025.png" loading="lazy"></figure>
<p>IndexedDB 是一种将数据持久存储在用户浏览器中的方法，它可以创建<strong>具有丰富查询能力</strong>的 Web 应用程序，并且<strong>可以在线和离线工作</strong>，它的<strong>存储空间比 Storage 大得多</strong>，一般来说不少于 250MB，甚至没有上限。IndexedDB 适用于存储大量数据的应用程序（例如，图书馆中的目录）和不需要持续互联网连接即可工作的应用程序（例如，邮件客户端、待办事项列表和记事本）。</p>
<h2 id="同源策略">同源策略</h2>
<ul>
<li>与大多数 Web 存储解决方案一样，IndexedDB 遵循同源策略。</li>
<li>可以访问相同域内存储的数据，但无法跨不同域访问数据。</li>
<li>第三方窗口内容（例如 <code>&lt;iframe&gt;</code> 内容）可以访问其嵌入来源的 IndexedDB 存储，除非浏览器设置为从不接受第三方 cookie。</li>
</ul>
<h2 id="面向对象的数据库">面向对象的数据库</h2>
<ul>
<li>IndexedDB 是一个基于 JavaScript 的面向对象数据库，IndexedDB 内部采用对象仓库（object store）存放数据。</li>
<li>允许存储和检索使用“键”索引的对象。</li>
<li>所有类型的数据都可以直接存入，包括结构化克隆算法支持的任何 JavaScript 对象都可以持久保存到该存储。</li>
<li>支持储存二进制数据（ArrayBuffer 对象和 Blob 对象）。</li>
<li>对象仓库中，数据以&quot;键值对&quot;的形式保存，每一个数据记录都有对应的主键，主键是独一无二的，不能有重复，否则会抛出错误。</li>
</ul>
<h2 id="nosql-数据库">NoSQL 数据库</h2>
<ul>
<li>IndexedDB 不是一个关系型数据库，在传统的关系数据库中，使用表来存储数据行和命名数据类型列的集合。</li>
<li>IndexedDB 不使用结构化查询语言 (SQL)，更接近 NoSQL 数据库。</li>
<li>它在生成游标的索引上使用查询，您可以使用游标遍历结果集。</li>
<li>IndexedDB 要求您为一种数据类型创建对象存储并将 JavaScript 对象持久保存到该存储。每个对象存储都可以有一个索引集合，可以高效地查询和迭代。</li>
</ul>
<h2 id="事务型">事务型</h2>
<ul>
<li>IndexedDB 属于事务型数据库系统，对数据库所做的所有更改都发生在事务中。这意味着一系列操作步骤之中，只要有一步失败，整个事务就都取消，数据库回滚到事务发生之前的状态，不存在只改写一部分数据的情况。</li>
<li>在 IndexedDB 中所做的一切都在事务上下文中发生，IndexedDB API 提供了许多代表索引、表、游标等的对象，但每个对象都与特定事务相关联。因此，不能在事务外执行命令或打开游标。</li>
<li>事务对象提供 error、abort 和 complete 三个事件，用来监听操作结果。</li>
<li>事务具有明确定义的生命周期，因此在事务完成后尝试使用它会抛出异常。</li>
<li>此外，事务自动提交，不能手动提交。例如，当同时在两个不同的选项卡中打开 Web 应用程序的两个实例时，如果没有事务操作，这两个实例可能会干扰彼此的修改。</li>
</ul>
<h2 id="异步">异步</h2>
<ul>
<li>IndexedDB API 大部分都是异步的。异步设计是为了防止大量数据的读写，拖慢网页的表现。</li>
<li>API 不会通过返回值提供数据；相反，它必须传递一个回调函数来获取返回值。</li>
<li>例如，很多请求具有 onsuccess 和 onerror 属性，也可以对它们调用 addEventListener() 和 removeEventListener()。</li>
<li>它们还有 readyState、result 和 errorCode 属性可以告诉您请求的状态。result 属性特别神奇，因为它可以是许多不同的东西，具体取决于请求的生成方式（例如，一个 IDBCursor 实例，或者您刚刚插入数据库的值的键）。</li>
</ul>
<h2 id="数据同步">数据同步</h2>
<ul>
<li>IndexedDB API 并非设计用于处理与服务器端数据库的同步。</li>
<li>如果需要与服务器端数据库的同步，可以编写将客户端 indexedDB 数据库与服务器端数据库同步的代码。</li>
</ul>
<h2 id="清除数据库">清除数据库</h2>
<p>注意，浏览器可以清除数据库，例如在以下情况下：</p>
<ul>
<li>浏览器的设置允许用户删除给定网站存储的所有数据，包括 cookie、书签、存储的密码和 IndexedDB 数据。</li>
<li>某些浏览器具有“隐私浏览”(Firefox) 或“隐身”(Chrome) 模式。在会话结束时，浏览器会清除数据库。</li>
<li>已达到磁盘或配额限制。</li>
<li>数据已损坏。</li>
<li>对该功能进行了不兼容的更改。</li>
</ul>
<h2 id="主要接口">主要接口</h2>
<h3 id="数据库idbdatabase-对象">数据库：IDBDatabase 对象</h3>
<ul>
<li>数据库是一系列相关数据的容器。每个域名（严格的说，是协议 + 域名 + 端口）都可以新建任意多个数据库。</li>
<li>IDBDatabase 接口提供一个到数据库的连接。</li>
<li>使用 IDBDatabase 对象在数据库中打开一个 transaction, 然后进行操作或者删除数据库中的对象。</li>
<li>IDBDatabase 接口是唯一一个能够访问和管理数据库版本的接口。</li>
<li>IndexedDB 数据库在同一个时刻，只能有一个版本的数据库存在。</li>
<li>如果要修改数据库结构（新增或删除表、索引或者主键），只能通过升级数据库版本完成。</li>
</ul>
<h3 id="对象仓库idbobjectstore-对象">对象仓库：IDBObjectStore 对象</h3>
<ul>
<li>每个数据库包含若干个对象仓库（object store）。它类似于关系型数据库的表格。</li>
<li>对象库中的记录根据其键值进行排序。这种排序可以实现快速插入，查找和有序检索。</li>
<li>对象仓库保存的是数据记录。每条记录类似于关系型数据库的行，但是只有主键和数据体两部分。主键用来建立默认的索引，必须是不同的，否则会报错。主键可以是数据记录里面的一个属性，也可以指定为一个递增的整数编号。数据体可以是任意数据类型，不限于对象。</li>
</ul>
<h3 id="索引-idbindex-对象">索引： IDBIndex 对象</h3>
<ul>
<li>为了加速数据的检索，可以在对象仓库里面，为不同的属性建立索引。</li>
<li>IDBIndex 接口提供了异步获取数据库中一个 index（被引用的 object store）的功能。</li>
<li>可以通过使用该接口来取回数据。IDBCursor 也提供了另一种方式。</li>
<li>一个 index 可以让你在 object store 的记录中，通过使用记录的 properties（属性）来寻找记录。</li>
<li>在 object store 中新增、更新或是删除记录时，索引中的记录将自动填充。</li>
<li>索引中的每条记录只能指向其引用的 object store 中的唯一一条记录，但是多个索引可以引用同一个 object store。</li>
<li>当 object store 变更时，所有引用 object store 的索引都会自动更新。</li>
</ul>
<h3 id="事务-idbtransaction-对象">事务： IDBTransaction 对象</h3>
<ul>
<li>异步事务使用数据库中的事件对象属性。</li>
<li>由 IDBDatabase 发起事务，通过 IDBTransaction 来设置事务的模式（例如：是否只读 readonly 或读写 readwrite）。</li>
<li>事务在被创建的时候就已经开始，并非在发起第一个请求（IDBRequest) 的时候。例如下面的例子，代码执行后，object store 应该具有值 &quot;2&quot;, 因为 trans2 应该在 trans1 之后执行。</li>
</ul>
<pre><code class="language-js">const trans1 = db.transaction(&quot;foo&quot;, &quot;readwrite&quot;);
const trans2 = db.transaction(&quot;foo&quot;, &quot;readwrite&quot;);
const objectStore2 = trans2.objectStore(&quot;foo&quot;)
const objectStore1 = trans1.objectStore(&quot;foo&quot;)
objectStore2.put(&quot;2&quot;, &quot;key&quot;);
objectStore1.put(&quot;1&quot;, &quot;key&quot;);
</code></pre>
<p>事务失败的情况：</p>
<ol>
<li>由于请求出错而中止，例如尝试 IDBObjectStore.add() 添加相同的键的记录两次，或 IDBObjectStore.put() 更新具有唯一性约束的相同索引键。这会导致请求错误，并会冒泡到事务错误，从而中止事务。这可以通过在 IDBRequest.error 事件上使用 preventDefault() 来防止。</li>
<li>显式调用 IDBTransaction.abort() 方法（回滚与此事务关联的数据库中对象的所有更改）。</li>
<li>IDBRequest 的 success 和 error 事件处理程序中的未捕获异常。</li>
<li>I/O 错误（例如实际写入磁盘失败，或其他操作系统/硬件故障）。</li>
<li>超出配额（存储）。</li>
<li>用户代理（浏览器）崩溃。</li>
</ol>
<h3 id="操作请求idbrequest-对象">操作请求：IDBRequest 对象</h3>
<ul>
<li>indexedDB 所有异步操作立即返回一个 IDBRequest 实例。</li>
<li>这个对象通过三个事件 error（打开数据库失败）、success（打开数据库成功）、upgradeneeded（打开数据库的版本号，大于数据库的实际版本号）来处理打开数据库的操作结果。</li>
<li>每一个请求都有一个 readyState 属性，初始时为 pending，当请求完成或失败的时候，readyState 会变为 done。当状态值变为 done 时，每一个请求都会返回 result 和 error 属性，并且会触发一个事件（success 或 error）。</li>
<li>当状态保持为 pending 时，任何尝试访问 result 或 error 属性的行为会触发一个 InvalidStateError 异常。</li>
</ul>
<pre><code class="language-js">const request = window.indexedDB.open(databaseName, version);
request.onsuccess = function (event) {
  db = request.result;
  console.log('数据库打开成功');
};
request.onupgradeneeded = function (event) {
  db = event.target.result;
}
</code></pre>
<h3 id="游标-idbcursor-对象">游标： IDBCursor 对象</h3>
<ul>
<li>遍历数据表格的多条记录，要使用指针对象 IDBCursor，它使应用程序能够异步处理在游标范围内的所有记录。</li>
<li>游标有一个 source 只读属性，指示游标正在迭代的索引（IDBIndex）或者对象存储区（IDBObjectStore）。</li>
<li>游标在所属区间范围内有一个位置，根据记录健（存储字段）的顺序递增或递减方向移动。</li>
<li>可以在同一时间拥有多个游标。</li>
</ul>
<pre><code class="language-js">function readAll() {
  const objectStore = db.transaction('person').objectStore('person');
  // openCursor()方法是一个异步操作，所以要监听success事件
   objectStore.openCursor().onsuccess = function (event) {
     const cursor = event.target.result;

     if (cursor) {
       console.log('Id: ' + cursor.key);
       console.log('Name: ' + cursor.value.name);
       console.log('Age: ' + cursor.value.age);
       console.log('Email: ' + cursor.value.email);
       cursor.continue();
    } else {
      console.log('没有更多数据了！');
    }
  };
}
</code></pre>
<h3 id="键集合idbkeyrange-对象">键集合：IDBKeyRange 对象</h3>
<ul>
<li>通过使用一个键或某个范围的键从 IDBObjectStore 和 IDBIndex 对象中检索记录。</li>
<li>可以使用下限和上限来限制范围。例如，可以遍历具有 A–Z 范围中的键的所有值。</li>
<li>下表给出了检索特定范围内的键的方法：</li>
</ul>
<table>
<thead>
<tr>
<th>特定范围</th>
<th>方法</th>
</tr>
</thead>
<tbody>
<tr>
<td>All keys ≥ x</td>
<td>IDBKeyRange.lowerBound (x)</td>
</tr>
<tr>
<td>All keys &gt; x</td>
<td>IDBKeyRange.lowerBound (x, true)</td>
</tr>
<tr>
<td>All keys ≤ y</td>
<td>IDBKeyRange.upperBound (y)</td>
</tr>
<tr>
<td>All keys &lt; y</td>
<td>IDBKeyRange.upperBound (y, true)</td>
</tr>
<tr>
<td>All keys ≥ x &amp;&amp; ≤ y</td>
<td>IDBKeyRange.bound (x, y)</td>
</tr>
<tr>
<td>All keys &gt; x &amp;&amp;&lt; y</td>
<td>IDBKeyRange.bound (x, y, true, true)</td>
</tr>
<tr>
<td>All keys &gt; x &amp;&amp; ≤ y</td>
<td>IDBKeyRange.bound (x, y, true, false)</td>
</tr>
<tr>
<td>All keys ≥ x &amp;&amp;&lt; y</td>
<td>IDBKeyRange.bound (x, y, false, true)</td>
</tr>
<tr>
<td>The key = z</td>
<td>IDBKeyRange.only (z)</td>
</tr>
</tbody>
</table>
<h2 id="indexeddb-库">IndexedDB 库</h2>
<p>localForage、dexie.js、PouchDB、idb、idb-keyval、JsStore、lovefield 等库使 IndexedDB 的使用对开发者来说更加友好。</p>
<h2 id="和-web-storage-存储对比">和 Web Storage 存储对比</h2>
<figure data-type="image" tabindex="2"><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/c97874be1f8446baa68ea7976264f9b2~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221203164453.png" loading="lazy"></figure>
<p>Web Storage 包含如下两种机制：</p>
<ol>
<li>sessionStorage 为每一个给定的源（origin）维持一个独立的存储区域，该存储区域在页面会话期间可用（即只要浏览器处于打开状态，包括页面重新加载和恢复）。</li>
<li>localStorage 同样的功能，但是在浏览器关闭，然后重新打开后数据仍然存在。</li>
</ol>
<pre><code class="language-js">localStorage.colorSetting = '#a4509b';
localStorage['colorSetting'] = '#a4509b';
localStorage.setItem('colorSetting', '#a4509b');
</code></pre>
<p>这三种方式都相同地设置了 colorSetting 条目。</p>
<p>对比如下：</p>
<ul>
<li>indexedDB 存储 IE10+ 支持，Storage 存储 IE8+ 支持，后者兼容性更好。</li>
<li>Storage 存储，结果每次写入都要字符串化，写出要解析成对象。使用 indexedDB 则无需数据转换。</li>
<li>indexedDB 可以在 Web Worker 中使用，Storage 存储在 Web Worker 中不可用。所以在进行 PWA 开发的时候，数据存储只能使用 indexedDB。</li>
</ul>
<p>如何选择：</p>
<ol>
<li>如果存储数据结构简单，例如基本类型，使用 Storage 存储更加方便。</li>
<li>如果数据结构比较复杂，同时对浏览器兼容性没要求，可以使用 indexedDB。</li>
<li>如果是在 Service Worker 中开发应用，只能使用 indexedDB 数据存储。</li>
</ol>
<h2 id="和-web-sql-对比">和 Web SQL 对比</h2>
<figure data-type="image" tabindex="3"><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/803c23a136b84b82b6fb55c7907a0547~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221202202657.png" loading="lazy"></figure>
<p>除了 IE 和 Firefox 不支持 Web SQL，Safari 从 13 开始也舍弃了 Web SQL，目前只有基于 Chromium 系列的浏览器继续支持。使用方式如下：</p>
<pre><code class="language-js">const db = openDatabase('mydb', '1.0', 'Test DB', 1024 * 10);
const msg = ['www.w3school.com.cn', 2];
db.transaction(function(tx) {
  tx.executeSql('CREATE TABLE IF NOT EXISTS LOGS (id unique, log)');
});
db.transaction(function(tx) {
  tx.executeSql('INSERT INTO LOGS (id, log) VALUES (1, &quot;mdn&quot;)');
  tx.executeSql('INSERT INTO LOGS (id, log) VALUES (2, &quot;https://developer.mozilla.org/&quot;)');
});
db.transaction(function(tx) {
  tx.executeSql('SELECT * FROM LOGS', [], console.log);
});
db.transaction(function(tx) {
  tx.executeSql('DELETE FROM LOGS  WHERE id=1', [], console.log);
});
db.transaction(function(tx) {
  tx.executeSql('UPDATE LOGS SET log=? WHERE id=?', msg, console.log);
});
</code></pre>
<p>上面这段代码即完成了建表、插入、查询、删除、更新数据的 Web SQL。可以看到直接把 SQL 语句嵌入到 JS 中了，与关系型数据库的操作非常类似。这个设计成为了 Web SQL 被舍弃的重要原因：</p>
<ul>
<li>学习成本高了很多，SQL 虽然本身并不复杂，但与前端跨度较大。</li>
<li>本身使用很不方便，需要把 JS 对象转换成关系型的字符串语句。</li>
</ul>
<p>详细对比表格：</p>
<table>
<thead>
<tr>
<th></th>
<th>Web SQL</th>
<th>IndexedDB</th>
</tr>
</thead>
<tbody>
<tr>
<td>优点</td>
<td>真正意义上的关系型数据库，类似于 SQLite（遵守 ACID 的轻型关系型数据库管理系统）。</td>
<td><li>允许对象的快速索引和搜索，因此在 Web 应用程序场景中，您可以非常快速地管理数据以及读取/写入数据。</li><li>由于是 NoSQL 数据库，因此我们可以根据实际需求设定我们的 JavaScript 对象和索引。</li><li>在异步模式下工作，每个事务具有适度的粒状锁。这允许您在 JavaScript 的事件驱动模块内工作。</li></td>
</tr>
<tr>
<td>不足</td>
<td><li>W3C 已弃用它，这意味着不再维护或开发它。</li><li>由于使用 SQL 语言，因此需要掌握 SQL 语法和转换 JavaScript 对象为对应的 SQL 语句。</li><li>非对象驱动。</li></td>
<td>API 复杂。</td>
</tr>
<tr>
<td>位置</td>
<td>包含行和列的表。</td>
<td>包含 JavaScript 对象和键的存储对象。</td>
</tr>
<tr>
<td>查询机制</td>
<td>SQL</td>
<td>Cursor APIs，Key Range APIs，应用程序代码</td>
</tr>
<tr>
<td>事务</td>
<td>锁可以发生在数据库，表，行的“读写”时候。</td>
<td>锁可以发生在数据库版本变更，或是存储对象“只读”和“读写”事务时候。</td>
</tr>
<tr>
<td>事务提交</td>
<td>事务创建是显式的。默认是回滚，除非我们调用提交。</td>
<td>事务创建是显式的。默认是提交，除非我们调用中止或有一个错误没有被捕获。</td>
</tr>
</tbody>
</table>
<h2 id="增删改查示例">增删改查示例</h2>
<p>参见 <a href="https://juejin.cn/post/7176211543330144315">前端本地存储数据库 IndexedDB 的增删改查</a></p>
<h2 id="参考资料">参考资料</h2>
<ul>
<li><a href="https://www.zhangxinxu.com/wordpress/2017/07/html5-indexeddb-js-example/">HTML5 indexedDB前端本地存储数据库实例教程</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[前端本地存储数据库 IndexedDB 的增删改查]]></title>
        <id>https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-de-zeng-shan-gai-cha/</id>
        <link href="https://zkrisj.gitee.io/post/qian-duan-ben-di-cun-chu-shu-ju-ku-indexeddb-de-zeng-shan-gai-cha/">
        </link>
        <updated>2022-12-12T10:32:37.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<figure data-type="image" tabindex="1"><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ff12ea5c5c484009a4776744a294b685~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221209222347.png" loading="lazy"></figure>
<p>IndexedDB 是一种浏览器底层 API，目前各浏览器都已支持，兼容性很好。虽然 Web Storage 在存储较少量的数据很有用，但对于存储更大量的结构化数据来说力不从心。而 IndexedDB 提供了这种场景的解决方案。</p>
<h2 id="特点">特点</h2>
<ol>
<li>IndexedDB 内部采用对象仓库（object store）存放键值对数据。可以存储结构化克隆算法支持的任何对象。每一个数据记录都有对应的主键，主键是独一无二的，并且不能有重复。</li>
<li>使用索引实现对数据的高性能搜索。</li>
<li>IndexedDB API 大部分都是异步的。不会通过返回值提供数据，而是要传递一个回调函数来获取返回值。</li>
<li>同大多数 Web 存储解决方案一样，IndexedDB 遵循同源策略。即可以访问相同域内存储的数据，但无法跨不同域访问数据。</li>
<li>可以存储文件/二进制对象。</li>
<li>在 Web Worker 中可用。</li>
</ol>
<p>IndexedDB 属于 NoSQL 和事务型面向对象数据库系统。对数据库的所有操作，都要通过事务完成。下面我们通过一个项目管理的增删改查来演示 IndexedDB API 的使用。</p>
<h2 id="连接数据库">连接数据库</h2>
<p>为了获取数据库的访问权限，需要在 window 对象的 indexedDB 属性上调用 open() 方法。该方法返回一个 IDBRequest 对象，异步操作通过在 IDBRequest 对象上触发事件来和调用进行通信。</p>
<pre><code class="language-js">const DBOpenRequest = window.indexedDB.open('project', 2);
</code></pre>
<p>连接数据库会在一个单独的线程中进行，包括以下几个步骤：</p>
<ol>
<li>指定数据库已经存在时：</li>
</ol>
<ul>
<li>等待 versionchange 操作完成。</li>
<li>如果数据库已计划删除，那等着删除完成。</li>
</ul>
<ol start="2">
<li>如果已有数据库版本高于给定的 version，中止操作并返回类型为 VersionError 的 DOMError。</li>
<li>如果已有数据库版本低于给定的 version，触发一个 versionchange 操作。</li>
<li>如果数据库不存在，创建指定名称的数据库，将版本号设置为给定版本，如果未给定版本号，则设置为 1。</li>
<li>创建数据库连接。</li>
</ol>
<blockquote>
<p>要注意的是，如果指定了版本号，后续的操作主要在 upgradeneeded 事件的监听函数里面完成。版本号不能使用浮点数，否则它将会被转变成离它最近的整数，这可能导致 upgradeneeded 事件不会被触发。</p>
<pre><code class="language-js">const request = indexedDB.open(&quot;MyTestDatabase&quot;, 2.4); // 不要这么做，因为版本会被置为 2。
</code></pre>
<p>我们对数据库某一行数据进行增加删除操作，我们是没有必要对数据库的版本号进行修改的。但是对于字段修改就不一样了，比方说原来是5列数据，我们现在改成6列，由于相关设置是在 onupgradeneeded 回调中，因此，这时我们需要增加版本号来触发字段修改。</p>
</blockquote>
<p>indexedDB.open() 方法会返回一个 IDBRequest 对象。这个对象通过三种事件 error、success、upgradeneeded，处理打开数据库的操作结果。</p>
<pre><code class="language-js">let db;
request.onerror = function (event) {
  console.log('数据库打开报错');
};
request.onsuccess = function (event) {
  db = request.result;
  console.log('数据库打开成功');
};
request.onupgradeneeded = function (event) {
  db = event.target.result;
};
</code></pre>
<h2 id="新建对象仓库新建表">新建对象仓库（新建表）</h2>
<p>IndexedDB 中新建对象仓库（新建表）需要使用 IDBDatabase 接口的 IDBDatabase.createObjectStore(name, options) 方法。</p>
<pre><code class="language-js">const objectStore = db.createObjectStore('project', { keyPath: 'id' });
</code></pre>
<ul>
<li>name
<ul>
<li>被创建的 object store 的名称。</li>
</ul>
</li>
<li>options
<ul>
<li>可选参数，其中包括以下的属性：</li>
<li>keyPath - 新对象存储要使用的主键属性，定义浏览器应从对象存储或索引中的何处提取主键。如果为空或未指定，则创建对象存储时没有主键并将使用外键。还可以传入一个数组作为 keyPath。</li>
<li>autoIncrement - 是否使用自动递增的整数作为主键（第一个数据记录为1，第二个数据记录为2，以此类推），默认为 false。一般来说，keyPath 和 autoIncrement 属性只要使用一个就够了。</li>
</ul>
</li>
</ul>
<h2 id="新建索引">新建索引</h2>
<p>IDBObjectStore.createIndex(indexName, keyPath, objectParameters) 法用来创建索引，可以理解为创建表字段，参数：</p>
<ul>
<li>indexName
<ul>
<li>创建的索引名称，可以使用空名称作为索引。</li>
</ul>
</li>
<li>keyPath
<ul>
<li>索引使用的键属性，可以使用空创建索引, 也可以传递数组。</li>
</ul>
</li>
<li>objectParameters
<ul>
<li>可选参数。常用参数之一是unique，表示该字段值是否唯一，不能重复。</li>
</ul>
</li>
</ul>
<h2 id="新增数据">新增数据</h2>
<p>向对象仓库写入数据记录需要通过事务完成。</p>
<ol>
<li>首先，新建一个事务。</li>
</ol>
<pre><code class="language-js">const transaction = db.transaction('project', &quot;readwrite&quot;);
</code></pre>
<ol start="2">
<li>打开存储对象。</li>
</ol>
<pre><code class="language-js">const objectStore = transaction.objectStore('project');
</code></pre>
<ol start="3">
<li>添加到数据对象中</li>
</ol>
<pre><code>const objectStoreRequest = objectStore.add(newItem);
</code></pre>
<ol start="4">
<li>使用一行语句表示就是：</li>
</ol>
<pre><code>db.transaction('project', &quot;readwrite&quot;).objectStore('project').add(newItem);
</code></pre>
<p>这里的 <code>newItem</code> 就是一个原生的纯粹的 JavaScript 对象，在本 demo 中，<code>newItem</code> 数据类似下面这样：</p>
<pre><code class="language-json">{
  &quot;name&quot;: &quot;第一个项目&quot;,
  &quot;begin&quot;: &quot;2022-12-12&quot;,
  &quot;end&quot;: &quot;2057-07-16&quot;,
  &quot;person&quot;: &quot;张三&quot;,
  &quot;remark&quot;: &quot;测试测试&quot;
}
</code></pre>
<h2 id="更新数据">更新数据</h2>
<ol>
<li>先根据 id 使用 objectStore.get(id) 方法获得对应行的存储对象。</li>
<li>再使用 objectStore.put(record) 进行数据库数据替换。</li>
</ol>
<pre><code class="language-js">// 新建事务
const transaction = db.transaction('project', &quot;readwrite&quot;);
// 打开已经存储的数据对象
const objectStore = transaction.objectStore(project);
// 获取存储的对应键的存储对象
const objectStoreRequest = objectStore.get(id);
// 获取成功后替换当前数据
objectStoreRequest.onsuccess = function(event) {
  // 当前数据
  const myRecord = objectStoreRequest.result;
  // 遍历替换
  for (const key in updateData) {
    if (typeof myRecord[key] != 'undefined') {
      myRecord[key] = data[key];
    }
  }
  // 更新数据库存储数据             
  objectStore.put(myRecord);
};
</code></pre>
<h2 id="删除数据">删除数据</h2>
<p>使用 IDBObjectStore.delete(key) 方法，和添加操作正好相反，但代码结构却是类似的。</p>
<pre><code class="language-js">const objectStoreRequest = db.transaction('project', &quot;readwrite&quot;).objectStore('project').delete(id);
</code></pre>
<h2 id="获取数据">获取数据</h2>
<p>IDBCursor 可以让我们一行一行读取数据库数据。</p>
<pre><code class="language-js">const objectStore = db.transaction(dbName).objectStore(dbName);
objectStore.openCursor().onsuccess = function(event) {
    const cursor = event.target.result;
    if (cursor) {
        // cursor.value就是数据对象
        // 游标没有遍历完，继续
        cursor.continue();
    } else {
        // 如果全部遍历完毕...
    }
}
</code></pre>
<p>可以看到，我们使用存储对象的 openCursor() 打开游标，在 onsuccess 回调中就可以遍历我们的游标对象了。其中 cursor.value 就是完整的数据对象，纯JS对象，就像下面这样：</p>
<pre><code class="language-json">{
  &quot;id&quot;: 1,
  &quot;name&quot;: &quot;第一个项目&quot;,
  &quot;begin&quot;: &quot;2022-12-12&quot;,
  &quot;end&quot;: &quot;2057-07-16&quot;,
  &quot;person&quot;: &quot;张三&quot;,
  &quot;remark&quot;: &quot;&quot;
}
</code></pre>
<p>IDBKeyRange 需要和 IDBCursor 一起使用。例如，只获取 id 从 4~10 之间的数据：</p>
<pre><code class="language-js">// 确定打开的游标的主键范围
const keyRangeValue = IDBKeyRange.bound(4, 10);
// 打开对应范围的游标
const objectStore = db.transaction(dbName).objectStore(dbName);
objectStore.openCursor(keyRangeValue).onsuccess = function(event) {
    const cursor = event.target.result;
    // ...
}
</code></pre>
<p>其中，有 bound()、only()、lowerBound()、upperBound() 这几个方法，意思就是方法名字面意思：“范围内”、“仅仅是”、“小于某值、“大于某值”。方法最后还支持两个布尔值参数，例如：</p>
<pre><code class="language-js">const keyRangeValue = IDBKeyRange.bound(4, 10, true, true);
</code></pre>
<p>则表示范围 3~9，布尔值参数为 true 的时候不含范围边界。</p>
<h2 id="查找记录">查找记录</h2>
<ol>
<li>IDBObjectStore 接口的 index(name) 方法在当前对象存储中打开一个命名索引，之后它可以用于，例如，使用游标返回按该索引排序的一系列记录。</li>
<li>IDBObjectStore.get(key) 用于从对象储存检索特定记录，它在单独的线程中返回由指定键选择的对象储存。</li>
<li>IDBObjectStore.getAll(query, count) 返回对象存储中与指定参数匹配的所有对象，如果没有给出参数，则返回存储中的所有对象。</li>
</ol>
<pre><code class="language-js">const store = db.transaction([dbName], 'readwrite').objectStore(dbName);
const request = store.index(name).getAll(value);
</code></pre>
<h2 id="完整代码马上掘金">完整代码+马上掘金</h2>
<iframe src="https://code.juejin.cn/pen/7176188633831964687"></iframe>
<h2 id="参考资料">参考资料</h2>
<ul>
<li><a href="https://www.zhangxinxu.com/wordpress/2017/07/html5-indexeddb-js-example/">HTML5 indexedDB前端本地存储数据库实例教程</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[纯 CSS 制作一个圣诞礼物动画]]></title>
        <id>https://zkrisj.gitee.io/post/chun-css-zhi-zuo-yi-ge-sheng-dan-li-wu-dong-hua/</id>
        <link href="https://zkrisj.gitee.io/post/chun-css-zhi-zuo-yi-ge-sheng-dan-li-wu-dong-hua/">
        </link>
        <updated>2022-12-12T10:31:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<p>CSS 动画包括两个部分：描述动画的样式规则和用于指定动画开始、结束以及中间点样式的关键帧。相较于传统的脚本实现动画技术，使用 CSS 动画有三个主要优点：</p>
<ol>
<li>能够非常容易地创建简单动画，你甚至不需要了解 JavaScript 就能创建动画。</li>
<li>动画运行效果良好，甚至在低性能的系统上。渲染引擎会使用跳帧或者其他技术以保证动画表现尽可能的流畅。而使用 JavaScript 实现的动画通常表现不佳（除非经过很好的设计）。</li>
<li>让浏览器控制动画序列，允许浏览器优化性能和效果，如降低位于隐藏选项卡中的动画更新频率。</li>
</ol>
<blockquote>
<p>创建动画序列，需要使用 animation 属性或其子属性，该属性允许配置动画时间、时长以及其他动画细节，但该属性不能配置动画的实际表现，动画的实际表现是由 @keyframes 规则实现。通过使用 @keyframes 建立两个或两个以上关键帧来实现动画。每一个关键帧都描述了动画元素在给定的时间点上应该如何渲染。动画的时间设置是通过 CSS 样式定义的，关键帧使用 percentage 来指定动画发生的时间点。0% 表示动画的第一时刻，100% 表示动画的最终时刻，这两个时间点十分重要，所以还有特殊的别名：from 和 to，若 from/0% 或 to/100% 未指定，则浏览器使用计算值开始或结束动画。也可包含 0%-100% 之间的关键帧，描述动画开始和结束之间的状态。</p>
</blockquote>
<p>圣诞节即将到来，Codepen 上面大佬使用纯 CSS 制作了一个圣诞礼物动画，效果如下：</p>
<figure data-type="image" tabindex="1"><img src="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/fc8e2805a2604daaa45a3d477be320d2~tplv-k3u1fbpfcp-watermark.image?" alt="CPT2212111726-366x366.gif" loading="lazy"></figure>
<h2 id="定义使用的-css-变量">定义使用的 CSS 变量</h2>
<pre><code class="language-css">:root {
  --red1: hsl(345, 100%, 48%);
  --red2: hsl(345, 100%, 38%);
  --white1: hsl(0, 0%, 100%);
  --white2: hsl(0, 0%, 90%);
  --blue: hsl(120, 100%, 20%);
}
</code></pre>
<ul>
<li>--red1：系带的颜色。</li>
<li>--red2：系带的阴影颜色。</li>
<li>-white1：盒子的颜色。</li>
<li>-white2：盒子的阴影颜色。</li>
<li>--blue：页面的背景颜色。</li>
</ul>
<h2 id="添加礼物容器-盒子和盒盖元素">添加礼物容器、盒子和盒盖元素</h2>
<pre><code class="language-html">&lt;div class=&quot;gift&quot;&gt;
  &lt;div class=&quot;gift__box&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;gift__lid&quot;&gt;&lt;/div&gt;
&lt;/div&gt;
</code></pre>
<p>然后，在盒子元素上面加上盒盖的阴影元素：</p>
<pre><code class="language-html">&lt;div class=&quot;gift__box&quot;&gt;
  &lt;div class=&quot;gift__lid-shadow&quot;&gt;&lt;/div&gt;
&lt;/div&gt;
</code></pre>
<p>设置礼物盒子、盒盖阴影、盒盖元素样式，盒子和盒盖上面的系带条纹由 linear-gradient 函数实现：</p>
<pre><code class="language-css">body {
  background-color: var(--blue);
  color: var(--white1);
  font: 1em/1.5 sans-serif;
  height: 100vh;
  display: grid;
  place-items: center;
  margin: 0;
}
.gift {
  position: relative;
  width: 18em;
  height: 18em;
}
.gift__box,
.gift__lid,
.gift__lid-shadow {
  position: absolute;
}
.gift__box,
.gift__lid,
.gift__lid-shadow {
  transform-origin: 50% 100%;
}
.gift__box {
  background:
    linear-gradient(var(--red2), var(--red2)) 50% 50% / 3.3em 100% no-repeat,
    var(--white2);
  border-radius: 1.5em;
  bottom: 0.5em;
  left: 3.3em;
  overflow: hidden;
  width: 11.4em;
  height: 9em;
}
.gift__lid,
.gift__lid-shadow {
  border-radius: 1em;
  width: 13em;
  height: 3.3em;
}
.gift__lid {
  background:
    linear-gradient(var(--red1), var(--red1)) 50% 50% / 3.3em 100% no-repeat,
    var(--white1);
  bottom: 8.7em;
  left: 2.5em;
}
.gift__lid-shadow {
  background-color: hsla(0, 0%, 0%, 0.1);
  top: -1.5em;
  left: -1em;
}
</code></pre>
<p>现在我们实现了礼物盒子和它的盒盖的形状：</p>
<figure data-type="image" tabindex="2"><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/3e75279d81f54f5284c9b843ae69e96f~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221211195413.png" loading="lazy"></figure>
<h2 id="添加系带元素">添加系带元素</h2>
<p>接下来我们给礼物盒子添加一个蝴蝶结系带，系带分为系带容器和左边、中间、右边元素。</p>
<pre><code class="language-html">&lt;div class=&quot;gift__bow&quot;&gt;
  &lt;div class=&quot;gift__bow-left&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;gift__bow-right&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;gift__bow-center&quot;&gt;&lt;/div&gt;
&lt;/div&gt;
</code></pre>
<p>设置样式，使用 CSS clip-path 属性设置蝴蝶结形状：</p>
<pre><code class="language-css">.gift__bow,
.gift__bow-center,
.gift__bow-left,
.gift__bow-right {
  position: absolute;
}
.gift__bow-center,
.gift__bow-left,
.gift__bow-right {
  background-color: var(--red2);
}
.gift__bow {
  bottom: 11em;
  left: 7.5em;
  width: 3em;
  height: 2em;
  transform-origin: 50% 230%;
}
.gift__bow-center {
  border-radius: 1em;
  width: 100%;
  height: 100%;
}
.gift__bow-left,
.gift__bow-right {
  box-shadow: 0 0 0 0.7em var(--red1) inset;
  top: 0.3em;
  width: 4em;
  height: 5em;
  z-index: -1;
}
.gift__bow-left:before,
.gift__bow-right:before {
  background-color: var(--red1);
  border-radius: inherit;
  content: &quot;&quot;;
  display: block;
  position: absolute;
  inset: 0;
}
.gift__bow-left {
  border-radius: 1.5em 0 3em 1em / 1.5em 0 3em 3.5em;
  right: calc(100% - 0.75em);
  transform: rotate(35deg);
  transform-origin: 100% 15%;
}
.gift__bow-left:before {
  clip-path: polygon(0 42%, 100% 12%, 100% 100%, 0 100%);
}
.gift__bow-right {
  border-radius: 0 1.5em 1em 3em / 0 1.5em 3.5em 3em;
  left: calc(100% - 0.75em);
  transform: rotate(-35deg);
  transform-origin: 0% 15%;
}
.gift__bow-right:before {
  clip-path: polygon(0 12%, 100% 42%, 100% 100%, 0 100%);
}
</code></pre>
<p>现在已经实现了一个完整的礼物图片了：</p>
<figure data-type="image" tabindex="3"><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f546db58f9a04ad5a65c2d2938027045~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221211200527.png" loading="lazy"></figure>
<h2 id="添加-css-动画">添加 CSS 动画</h2>
<p>设置动画时长 CSS 变量，分别给礼物盒子、盒盖阴影、盒盖元素、系带容器和左边、右边元素添加 CSS 动画：</p>
<pre><code class="language-css">.gift {
  --dur: 1.5s;
}
.gift__bow {
  animation: bowBounce var(--dur) ease-in-out infinite;
}
.gift__bow-left {
  animation: bowLeftPivot var(--dur) ease-in-out infinite;
}
.gift__bow-right {
  animation: bowRightPivot var(--dur) ease-in-out infinite;
}
.gift__box {
  animation: boxBounce var(--dur) ease-in-out infinite;
}
.gift__lid {
  animation: lidBounce var(--dur) ease-in-out infinite;
}
.gift__lid-shadow {
  animation: lidShadowBounce var(--dur) ease-in-out infinite;
}
@keyframes bowBounce {
  from,
  50% {
    transform: translateY(0) rotate(0);
  }
  62.5% {
    animation-timing-function: ease-in;
    transform: translateY(75%) rotate(0);
  }
  68.75% {
    animation-timing-function: ease-out;
    transform: translateY(-37.5%) rotate(15deg);
  }
  75% {
    animation-timing-function: ease-in-out;
    transform: translateY(-150%) rotate(5deg);
  }
  87.5% {
    transform: translateY(65%) rotate(-3deg);
  }
  to {
    transform: translateY(0) rotate(0);
  }
}
@keyframes bowLeftPivot {
  from,
  50% {
    transform: rotate(35deg);
  }
  62.5% {
    transform: rotate(45deg);
  }
  75% {
    transform: rotate(30deg);
  }
  87.5% {
    transform: rotate(45deg);
  }
  to {
    transform: rotate(35deg);
  }
}
@keyframes bowRightPivot {
  from,
  50% {
    transform: rotate(-35deg);
  }
  62.5% {
    transform: rotate(-45deg);
  }
  75% {
    transform: rotate(-34deg);
  }
  87.5% {
    transform: rotate(-45deg);
  }
  to {
    transform: rotate(-35deg);
  }
}
@keyframes boxBounce {
  from,
  50% {
    transform: translateY(0) scale(1, 1);
  }
  62.5% {
    transform: translateY(4%) scale(1.12, 0.89);
  }
  75% {
    transform: translateY(-11%) scale(0.92, 1.1);
  }
  87.5% {
    transform: translateY(0) scale(1.05, 0.9);
  }
  to {
    transform: translateY(0) scale(1, 1);
  }
}
@keyframes lidBounce {
  from,
  50% {
    transform: translateY(0) scale(1, 1) rotate(0);
  }
  62.5% {
    animation-timing-function: ease-in;
    transform: translateY(45%) scale(1.14, 0.95) rotate(0);
  }
  68.75% {
    animation-timing-function: ease-out;
    transform: translateY(-22.5%) scale(1.05, 1.03) rotate(15deg);
  }
  75% {
    animation-timing-function: ease-in-out;
    transform: translateY(-90%) scale(0.96, 1.1) rotate(5deg);
  }
  87.5% {
    transform: translateY(30%) scale(1.12, 0.93) rotate(-3deg);
  }
  to {
    transform: translateY(0) scale(1, 1) rotate(0);
  }
}
@keyframes lidShadowBounce {
  from,
  50% {
    transform: translateY(0) scale(1, 1) rotate(0);
  }
  62.5% {
    animation-timing-function: ease-in;
    transform: translateY(10%) scale(1.14, 0.95) rotate(0);
  }
  68.75% {
    animation-timing-function: ease-out;
    transform: translateY(-10%) scale(1.05, 1.03) rotate(15deg);
  }
  75% {
    animation-timing-function: ease-in-out;
    transform: translateY(-30%) scale(0.96, 1.1) rotate(5deg);
  }
  87.5% {
    transform: translateY(10%) scale(1.12, 0.93) rotate(-3deg);
  }
  to {
    transform: translateY(0) scale(1, 1) rotate(0);
  }
}
</code></pre>
<p>现在我们已经完成了一个完整的礼物图片和循环的弹起动画了，为了让圣诞礼物看起来更炫酷、崭新的样子，可以添加几个闪烁的星星来衬托这个圣诞礼物。</p>
<pre><code class="language-html">&lt;div class=&quot;gift__star gift__star--1&quot;&gt;&lt;/div&gt;
&lt;div class=&quot;gift__star gift__star--2&quot;&gt;&lt;/div&gt;
&lt;div class=&quot;gift__star gift__star--3&quot;&gt;&lt;/div&gt;
&lt;div class=&quot;gift__star gift__star--4&quot;&gt;&lt;/div&gt;
&lt;div class=&quot;gift__star gift__star--5&quot;&gt;&lt;/div&gt;
</code></pre>
<p>使用 CSS clip-path 属性设置五角星形状：</p>
<pre><code class="language-css">.gift__star {
  animation: starRotateCW var(--dur) ease-in infinite;
  background-color: var(--white1);
  clip-path: polygon(50% 0, 65% 35%, 100% 50%, 65% 65%, 50% 100%, 35% 65%, 0 50%, 35% 35%);
  transform: scale(0);
}
.gift__star--2,
.gift__star--4,
.gift__star--5 {
  animation-name: starRotateCCW;
}
.gift__star--1 {
  animation-delay: calc(var(--dur) * 0.5);
  top: 0;
  left: 12.5em;
  width: 1.5em;
  height: 1.5em;
}
.gift__star--2 {
  animation-delay: calc(var(--dur) * 0.125);
  top: 2em;
  left: 10em;
  width: 1.75em;
  height: 1.75em;
}
.gift__star--3 {
  animation-delay: calc(var(--dur) * 0.25);
  top: 8em;
  left: 0;
  width: 1.25em;
  height: 1.25em;
}
.gift__star--4 {
  top: 10.5em;
  right: 0;
  width: 1.75em;
  height: 1.75em;
}
.gift__star--5 {
  animation-delay: calc(var(--dur) * 0.375);
  top: 12em;
  left: 1.8em;
  width: 2.5em;
  height: 2.5em;
}
</code></pre>
<p>添加五角星闪烁的动画：</p>
<pre><code class="language-css">@keyframes starRotateCW {
  from {
    transform: scale(0) rotate(0);
  }
  25% {
    animation-timing-function: ease-out;
    transform: scale(1) rotate(0.25turn);
  }
  50%,
  to {
    transform: scale(0) rotate(0.5turn);
  }
}
@keyframes starRotateCCW {
  from {
    transform: scale(0) rotate(0);
  }
  25% {
    animation-timing-function: ease-out;
    transform: scale(1) rotate(-0.25turn);
  }
  50%,
  to {
    transform: scale(0) rotate(-0.5turn);
  }
}
</code></pre>
<h2 id="完整代码马上掘金">完整代码+马上掘金</h2>
<iframe src="https://code.juejin.cn/pen/7175827138870771771"></iframe>]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[长文，Vue3+Vite 中 JSX 的使用方式]]></title>
        <id>https://zkrisj.gitee.io/post/chang-wen-vue3vite-zhong-jsx-de-shi-yong-fang-shi/</id>
        <link href="https://zkrisj.gitee.io/post/chang-wen-vue3vite-zhong-jsx-de-shi-yong-fang-shi/">
        </link>
        <updated>2022-12-07T16:01:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<p>JSX（JavaScript 和 XML），是一个 HTML-in-JavaScript 的语法扩展，首先在 React 中被进入。JSX 可以很好地描述 UI 应该呈现出它应有交互的本质形式。JSX 是在 JavaScript 语法上的拓展，因此类似于 HTML 的代码可以和 JS 共存。例如：</p>
<pre><code class="language-js">const button = &lt;MyButton color=&quot;blue&quot; shadowSize={2}&gt;
  Click Me
&lt;/MyButton&gt;
</code></pre>
<p>该 button 常量称为 JSX 表达式。可以使用它在我们的应用程序中渲染 <code>&lt;MyButton&gt;</code> 标签。浏览器是无法读取直接解析 JSX 的。JSX 表达式经过（ Babel 或 Parcel 之类的工具）编译之后是这样的：</p>
<pre><code class="language-js">React.createElement(
  MyButton,
  {color: 'blue', shadowSize: 2},
  'Click Me'
)
</code></pre>
<p>实际上，JSX 仅仅只是 React.createElement(component, props, ...children) 函数的语法糖。可以使用 React.createElement() 自己编写 UI 来跳过编译步骤。但是，这样做会失去 JSX 的声明性优势，并且代码变得更难以阅读。编译是开发过程中的一个额外步骤，但是 React 社区中的许多开发人员都认为 JSX 的可读性值得。另外，流行的工具使 JSX-to-JavaScript 编译成为其设置过程的一部分。除非您愿意，否则不必自己配置编译。如果你想测试一些特定的 JSX 会转换成什么样的 JavaScript，你可以尝试使用 <a href="https://babeljs.io/repl/#?presets=react&amp;code_lz=GYVwdgxgLglg9mABACwKYBt1wBQEpEDeAUIogE6pQhlIA8AJjAG4B8AEhlogO5xnr0AhLQD0jVgG4iAXyJA">在线的 Babel 编译器</a>。</p>
<p>React 并不强制要求使用 JSX。当你不想在构建环境中配置有关 JSX 编译时，不在 React 中使用 JSX 会更加方便。例如，用 JSX 编写的代码：</p>
<pre><code class="language-js">class Hello extends React.Component {
  render() {
    return &lt;div&gt;Hello {this.props.toWhat}&lt;/div&gt;;
  }
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(&lt;Hello toWhat=&quot;World&quot; /&gt;);
</code></pre>
<p>可以编写为不使用 JSX 的代码：</p>
<pre><code class="language-js">class Hello extends React.Component {
  render() {
    return React.createElement('div', null, `Hello ${this.props.toWhat}`);
  }
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(React.createElement(Hello, {toWhat: 'World'}, null));
</code></pre>
<h2 id="在-vue3-中使用-jsx">在 Vue3 中使用 JSX</h2>
<p>Vue 使用单文件组件，把 template 模板、相关脚本和 CSS 一起整合放在 .vue 结尾的一个单文件中。这些文件最终会通过 JS 打包或构建工具（例如 Webpack、Vite）处理。</p>
<blockquote>
<p><code>&lt;template&gt;</code> 元素包含了所有的标记结构和组件的展示逻辑。template 可以包含任何合法的 HTML，以及 Vue 特定的语法。通过设置 <code>&lt;template&gt;</code> 标签的 <code>lang</code> 属性，例如可以通过设置 <code>&lt;template lang=&quot;pug&quot;&gt;</code> 就可以在使用 Pug 模板来替代标准 HTML。</p>
<p>而 .vue 文件中的 <code>&lt;script&gt;</code> 标签包含组件中所有的非显示逻辑，并且需要默认导出一个 JS 对象。该对象是在本地注册组件、定义属性、处理本地状态、定义方法等的地方。在构建阶段这个包含了 template 模板的对象会被处理和转换成为一个有 render() 函数的 Vue 组件。</p>
<p>组件的 CSS 样式写在 <code>&lt;style&gt;</code> 标签里，如果添加了 <code>scoped</code> 属性，Vue 会把样式的范围限制到单文件组件的内容里。这是类似于 CSS-in-JS 的解决方案，只不过允许书写纯粹的 CSS。如果通过 CLI 创建项目时选择了 CSS 预处理器，则可以将 <code>lang</code> 属性添加到 <code>&lt;style&gt;</code> 标签中，以便 Webpack 可以在构建时处理内容。</p>
</blockquote>
<p>虽然 jsx 最早是由 React 引入，但实际上 JSX 语法并没有定义运行时语义，并且能被编译成各种不同的输出形式。如果你之前使用过 JSX 语法，那么请注意 <strong>Vue 的 JSX 编译方式与 React 中 JSX 的编译方式不同</strong>，因此不能在 Vue 应用中使用 React 的 JSX 编译。与 React JSX 语法的一些明显区别包括：</p>
<ul>
<li>可以使用 HTML attributes 比如 <code>class</code> 和 <code>for</code> 作为 props - 不需要使用 <code>className</code> 或 <code>htmlFor</code>。</li>
<li>传递子元素给组件 (比如 slots) 的<a href="https://cn.vuejs.org/guide/extras/render-function.html#passing-slots">方式不同</a>。</li>
</ul>
<p>Vue 的类型定义也提供了 TSX 语法的类型推导支持。当使用 TSX 语法时，确保在 <code>tsconfig.json</code> 中配置了 <code>&quot;jsx&quot;: &quot;preserve&quot;</code>，这样的 TypeScript 就能保证 Vue JSX 语法编译过程中的完整性。</p>
<h3 id="安装插件vitejsplugin-vue-jsx">安装插件（@vitejs/plugin-vue-jsx）</h3>
<p>vite 官方提供了官方的插件来支持在 vue3 中使用 jsx/tsx，直接安装就行。</p>
<pre><code>npm i @vitejs/plugin-vue-jsx -D
</code></pre>
<p>安装完之后在 vite.config.js 文件中的 plugins 字段中添加 jsx 支持：</p>
<pre><code class="language-js">import vueJsx from &quot;@vitejs/plugin-vue-jsx&quot;;

export default defineConfig({
  plugins: [
    vueJsx(),
  ]
})
</code></pre>
<p>这样就可以在项目中使用 jsx/tsx 了。</p>
<h3 id="新建-jsx-文件">新建 jsx 文件</h3>
<p>在项目中新建 jsx 或 tsx 后缀的文件，语法和 js 文件类似，但是和 .vue 文件中的 <code>&lt;script&gt;</code> 标签一样，jsx 文件模块需要默认导出一个 JS 对象。该对象用来在本地注册组件、定义属性、处理本地状态、定义方法等。</p>
<pre><code class="language-js">import HelloWorld from './HelloWorld.vue'
export default {
  setup() {
    return () =&gt; &lt;HelloWorld msg=&quot;11&quot; /&gt;;
  },
};
</code></pre>
<h3 id="语法">语法</h3>
<ol>
<li>插值。与 vue 模板语法中的插值一样，但是双大括号 {{}} 变为了单大括号 {}。大括号内支持任何有效的 JavaScript 表达式，比如：2 + 2，user.firstName，formatName(user) 等。</li>
</ol>
<pre><code class="language-html">// 模板语法
&lt;span&gt;{{ a + b }}&lt;/span&gt;

// jsx/tsx
&lt;span&gt;{ a + b }&lt;/span&gt;
</code></pre>
<ol start="2">
<li>class 类名绑定。有两种方式，使用模板字符串或者使用数组。</li>
</ol>
<pre><code class="language-html">// 模板字符串
&lt;div className={ `header ${ isBg ? 'headerBg' : '' }` }&gt;header&lt;/div&gt;
// 数组
&lt;div class={ [ 'header', isBg &amp;&amp; 'headerBg' ] } &gt;header&lt;/div&gt;
</code></pre>
<ol start="3">
<li>style 样式绑定。需要使用双大括号。</li>
</ol>
<pre><code class="language-jsx">const color = 'red'
const element = &lt;sapn style={{ color, fontSize: '16px' }}&gt;style&lt;/sapn&gt;
</code></pre>
<ol start="4">
<li>条件渲染。由于 jsx 本身具有 js 语法，所以不再需要使用 v-if 指令，使用 if/else 和三元表达式都可以实现。但是支持 v-show 指令。</li>
</ol>
<pre><code class="language-js">const element = (name) =&gt; {
  if (name) {
    return &lt;h1&gt;Hello, { name }&lt;/h1&gt;
  } else {
    return &lt;h1&gt;Hello, Stranger&lt;/h1&gt;
  }
}

const element = icon ? &lt;span class=&quot;icon&quot;&gt;&lt;/span&gt; : null;
// 以上代码等效于：
const element = icon &amp;&amp; &lt;span class=&quot;icon&quot;&gt;&lt;/span&gt;;
</code></pre>
<ol start="5">
<li>列表渲染。同样，由于 jsx 本身具有 js 语法，所以不再需要使用 v-for 指令，使用 JS 数组的 map 方法即可。</li>
</ol>
<pre><code class="language-js">const listData = [
   {name: 'Tom', age: 18},
   {name: 'Jim', age: 20},
   {name: 'Lucy', age: 16}
]
return () =&gt; (
   &lt;div&gt;
     &lt;div class={'box'}&gt;
       &lt;span&gt;姓名&lt;/span&gt;
       &lt;span&gt;年龄&lt;/span&gt;
     &lt;/div&gt;
   {
   prop.listData.map(item =&gt; &lt;div class={'box'}&gt;
       &lt;span&gt;{item.name}&lt;/span&gt;
       &lt;span&gt;{item.age}&lt;/span&gt;
     &lt;/div&gt;
     })
   &lt;/div&gt;
)
</code></pre>
<ol start="6">
<li>标签属性绑定。也是使用大括号包裹，不能使用 v-bind 指令。而 vue 组件中通过 <code>&lt;div v-bind=&quot;properties&quot;&gt;&lt;/div&gt;</code> 批量绑定标签属性，在 JSX 中需要使用 <code>&lt;div {...properties}&gt;&lt;/div&gt;</code>。</li>
</ol>
<pre><code class="language-js">const href = 'https://cn.vuejs.org/'
const element = &lt;a href={href}&gt;Vue3&lt;/a&gt;
</code></pre>
<ol start="7">
<li>事件绑定。使用的也是 单大括号 {}，不过事件绑定不是以 @为前缀了，而是改成了 on，与原生相同。例如：click 事件是 onClick 或 onclick。</li>
</ol>
<pre><code class="language-js">const confirm = () =&gt; {
  // 确认提交
}
&lt;button onClick={confirm}&gt;确定&lt;/button&gt;
</code></pre>
<p>如果要带参数，需要使用箭头函数进行包裹：</p>
<pre><code class="language-js">const confirm = (name) =&gt; {
  // 确认提交
}
&lt;button onClick={() =&gt; confirm('Are you sure')}&gt;确定&lt;/button&gt;
</code></pre>
<ol start="8">
<li>事件修饰符。需要使用 withModifiers 方法，接收两个参数，第一个参数是绑定的事件，第二个参数是需要使用的事件修饰符。</li>
</ol>
<pre><code class="language-js">import { withModifiers, defineComponent, ref } from 'vue'

const App = defineComponent({
  setup() {
    const count = ref(0);

    const inc = () =&gt; {
      count.value++;
    };

    return () =&gt; (
      &lt;div onClick={ withModifiers(inc, ['self']) }&gt;{ count.value }&lt;/div&gt;
    );
  },
})
export default App
</code></pre>
<p>注意：Vue 模板中 ref 变量是可以直接解构的，但是在 jsx 中不行，需要添加 .value，比如上面的 { count.value }。</p>
<ol start="9">
<li>v-model 双向绑定。需要使用单大括号 {}。如果绑定属性则需要一个数组，第一个元素为绑定的值，第二个元素为绑定的属性。</li>
</ol>
<pre><code class="language-js">// 绑定值
&lt;input v-model=&quot;show&quot; /&gt; // vue
&lt;input v-model={show.value} /&gt; // jsx

// 绑定属性
&lt;input v-model:prop=&quot;show&quot; /&gt; // vue
&lt;input v-model={[show.value,'prop']} /&gt; // jsx

// 修饰符写法
&lt;input v-model:prop.trim=&quot;show&quot; /&gt; // vue
&lt;input v-model={[show.value,'prop',['trim']]} /&gt; // jsx
</code></pre>
<ol start="10">
<li>slot 插槽。jsx/tsx 中无法使用 slot 标签，定义插槽方式一：通过 setup 函数的第一个参数 ctx 上下文对象的 slots 的属性，setup 函数默认接收两个参数：</li>
</ol>
<ul>
<li>props - 组件传入的参数对象。</li>
<li>ctx - 上下文对象，上下文对象暴露了其他一些在 setup 中可能会用到的值，包括：
<ul>
<li>attrs - 透传的 Attributes（非响应式的对象，等价于 $attrs）。</li>
<li>slots - 插槽（非响应式的对象，等价于 $slots）。</li>
<li>emit - 触发事件的函数（等价于 $emit）。</li>
<li>expose - 暴露公共属性的函数。</li>
</ul>
</li>
</ul>
<p>如果解构了 <code>props</code> 对象，解构出的变量将会丢失响应性，因此推荐通过 <code>props.xxx</code> 的形式来使用其中的 props。如果确实需要解构 <code>props</code> 对象，或者需要将某个 prop 传到一个外部函数中并保持响应性，可以使用 <a href="https://cn.vuejs.org/api/reactivity-utilities.html#torefs">toRefs()</a> 和 <a href="https://cn.vuejs.org/api/reactivity-utilities.html#toref">toRef()</a> 这两个工具函数：</p>
<pre><code class="language-js">import { toRefs, toRef } from 'vue'

export default {
  setup(props) {
    // 将 `props` 转为一个其中全是 ref 的对象，然后解构
    const { title } = toRefs(props)
    // `title` 是一个追踪着 `props.title` 的 ref
    console.log(title.value)

    // 或者，将 `props` 的单个属性转为一个 ref
    const title = toRef(props, 'title')
  }
}
</code></pre>
<p>ctx 上下文对象是非响应式的，可以安全地解构：</p>
<pre><code class="language-js">export default {
  setup(props, { attrs, slots, emit, expose }) {
    ...
  }
}
</code></pre>
<p>attrs 和 slots 都是响应式（有状态）的对象，它们总是会随着组件自身的更新而更新。这意味着你应当避免解构它们，并始终通过 attrs.x 或 slots.x 的形式使用其中的属性。此外，和 props 不同，attrs 和 slots 的属性都不是响应式的。如果想要基于 attrs 或 slots 的改变来执行副作用，那么应该在 onBeforeUpdate 生命周期钩子中编写相关逻辑。</p>
<p>expose 函数用于显式地限制该组件暴露出的属性，当父组件通过模板引用访问该组件的实例时，将仅能访问 expose 函数暴露出的内容：</p>
<pre><code class="language-js">export default {
  setup(props, { expose }) {
    // 让组件实例处于 “关闭状态”
    // 即不向父组件暴露任何东西
    expose()

    const publicCount = ref(0)
    const privateCount = ref(0)
    // 有选择地暴露局部状态
    expose({ count: publicCount })
  }
}
</code></pre>
<p>通过 ctx 上下文对象的 slots 的属性获取插槽对象后，就可以定义插槽了。</p>
<pre><code class="language-js">import { defineComponent } from 'vue'

export default defineComponent({
  setup(props, { slots }) { // 逻辑
    return () =&gt; {
      return &lt;p&gt;
     &lt;button&gt;{ slots.test?.() }&lt;/button&gt;
     &lt;button&gt;{ slots.default?.() }&lt;/button&gt;
     &lt;/p&gt;
    }
  },
})

// 在引用的组件中
&lt;template #test&gt;slot-test&lt;/template&gt;
&lt;template #&gt;slot-default&lt;/template&gt;
</code></pre>
<p>定义插槽方式二：使用 renderSlot 函数。</p>
<pre><code class="language-js">import { renderSlot } from 'vue'

&lt;button&gt;
  { renderSlot(slots, 'default') }
&lt;/button&gt;
</code></pre>
<p>而如果在 jsx 中使用插槽，可以直接通过标签属性 slot，或通过 v-slots 指令。</p>
<pre><code class="language-js">import HelloWorld from './HelloWorld'
export default defineComponent({
  setup() {
    return () =&gt; &lt;div class={'box'}&gt;
      &lt;HelloWorld v-slots={{
        title: () =&gt; {
          return &lt;p&gt;我是title插槽&lt;/p&gt;
        },
        default: () =&gt; {
          return &lt;p&gt;我是default插槽&lt;/p&gt;
         }
      }} /&gt;
    &lt;/div&gt;
  }
})
</code></pre>
<ol start="11">
<li>CSS Modules。引入局部样式，相当于 vue 组件中 <code>&lt;style&gt;</code> 标签的 scoped 属性。</li>
</ol>
<pre><code class="language-js">import styles from './index.module.scss'

&lt;div class={styles.wrap}&gt;&lt;/div&gt;
</code></pre>
<h2 id="github-源码">GitHub 源码</h2>
<ul>
<li><a href="https://github.com/zkrisj/vue-defineCustomElement">Vue3 中自定义组件</a></li>
</ul>
<h2 id="参考资料">参考资料</h2>
<ul>
<li><a href="https://mp.weixin.qq.com/s/FtVZEdkrXLSfJpPYaToAYQ">在 vue3 中优雅的使用 jsx/tsx</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[✏️修改常见的原生表单控件的默认样式]]></title>
        <id>https://zkrisj.gitee.io/post/xiu-gai-chang-jian-de-yuan-sheng-biao-dan-kong-jian-de-mo-ren-yang-shi/</id>
        <link href="https://zkrisj.gitee.io/post/xiu-gai-chang-jian-de-yuan-sheng-biao-dan-kong-jian-de-mo-ren-yang-shi/">
        </link>
        <updated>2022-12-06T16:40:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<p>HTML 原生的表单控件样式在各个浏览器上面由用户代理默认设置样式，如果在页面上应用了其他颜色或主题时，我们通常也相应的更改这些输入框或按钮的颜色或背景，否则会出现颜色与背景或主题不融入的样式不一致问题。</p>
<p>一般而言，HTML 中表单控件的效果都是通过浏览器的 Shadow Dom 创建的，脱离文档主树，不受大环境 CSS 影响，要控制其 UI 只能使用浏览器开发的伪元素 API 接口。并且，只有部分的样式可以重置。而其余的默认样式我们是无法使用浏览器自带的开发工具查看到的，因为其并不属于文档树，只是背后的一棵子树。</p>
<p>针对特定表单控件，浏览器提供了特定的伪元素用来改变样式。</p>
<h2 id="inputtypesearch">input[type=search]</h2>
<p>有如下 2 个伪元素可以改变搜索控件的 UI：</p>
<ul>
<li>::-webkit-search-cancel-button - 搜索框右边的清除按钮，用来清除输入内容，在有内容时才显示。</li>
<li>::-webkit-search-results-button - 搜索框左边的菜单按钮，用来显示历史记录，现在浏览器已经不显示了。</li>
</ul>
<iframe src="https://code.juejin.cn/pen/7173599338621796388"></iframe>
<h2 id="inputtypenumber">input[type=number]</h2>
<p>有如下伪元素可以改变数字控件的 UI：</p>
<ul>
<li>::-webkit-textfield-decoration-container – 填写数值的外部容器。</li>
<li>::-webkit-inner-spin-button – 上下箭头。</li>
</ul>
<iframe src="https://code.juejin.cn/pen/7173566521082380318"></iframe>
<h2 id="inputtyperange">input[type=range]</h2>
<p>有如下 2 个伪元素可以改变滑块控件的 UI：</p>
<ul>
<li>::-webkit-slider-runnable-track - 滑块凹槽，也就是滑块可滑动的区域。</li>
<li>::-webkit-slider-thumb - 滑块的具体样式，该伪类需要配合 ::-webkit-slider-runnable-track 使用，否则会没有效果。</li>
</ul>
<iframe src="https://code.juejin.cn/pen/7173586419850412032"></iframe>
<h2 id="inputtypecolor">input[type=color]</h2>
<p>有如下 2 个伪元素可以改变颜色选择控件的 UI：</p>
<ul>
<li>::-webkit-color-swatch-wrapper - 颜色选择控件的容器元素。</li>
<li>::-webkit-color-swatch - 颜色选择控件的指示颜色。</li>
</ul>
<iframe src="https://code.juejin.cn/pen/7173550533599969280"></iframe>
<h2 id="inputtypedate">input[type=date]</h2>
<p>有如下 8 个伪元素可以改变日期控件的 UI：</p>
<ul>
<li>::-webkit-datetime-edit – 控制编辑区域。</li>
<li>::-webkit-datetime-edit-fields-wrapper – 控制年月日这个区域。</li>
<li>::-webkit-datetime-edit-text – 控制年月日之间的斜线或短横线。</li>
<li>::-webkit-datetime-edit-month-field – 控制月份。</li>
<li>::-webkit-datetime-edit-day-field – 控制日期。</li>
<li>::-webkit-datetime-edit-year-field – 控制年份。</li>
<li>::-webkit-inner-spin-button – 控制上下小箭头。</li>
<li>::-webkit-calendar-picker-indicator – 控制下拉小箭头。</li>
</ul>
<iframe src="https://code.juejin.cn/pen/7173556984266883109"></iframe>
<h2 id="单选框-复选框-下拉框-文件选择框">单选框、复选框、下拉框、文件选择框</h2>
<ul>
<li>单选框、复选框、下拉框控件的样式修改参见：<a href="https://juejin.cn/post/7173701016666243109">如何更简单地修改单选框、复选框、下拉框的样式</a></li>
<li>文件选择框控件的样式修改参见：<a href="https://juejin.cn/post/7171015630269710349">如何更简单地更改 input type=file 文件选择框的样式</a></li>
</ul>
<h2 id="meter">meter</h2>
<p>用做测量值、评分等。有如下伪元素可用：</p>
<ul>
<li>::-webkit-meter-bar – 背景。</li>
<li>::-webkit-meter-optimum-value – 得分好的时候那部分区域状态。</li>
<li>::-webkit-meter-suboptimal-value – 分数凑合的时候区域状态。</li>
<li>::-webkit-meter-even-less-good-value – 分数糟糕的时候区域状态。</li>
</ul>
<iframe src="https://code.juejin.cn/pen/7173659858648956960"></iframe>
<h2 id="progress">progress</h2>
<p>用来显示一项任务的完成进度。有如下伪元素可用：</p>
<ul>
<li>::-webkit-progress-inner-element – 选择 <code>&lt;progress&gt;</code> 元素。</li>
<li>::-webkit-progress-bar – 背景进度条。</li>
<li>::-webkit-progress-value – 完成进度条。</li>
</ul>
<p>其中，::-webkit-progress-bar 是::-webkit-progress-inner-element 伪元素的子元素，同时是 ::-webkit-progress-value 伪元素的父元素。</p>
<iframe src="https://code.juejin.cn/pen/7173659869856481288"></iframe>
<h2 id="参考资料">参考资料</h2>
<ul>
<li><a href="https://www.zhangxinxu.com/wordpress/2013/06/%e4%bc%aa%e5%85%83%e7%b4%a0-%e8%a1%a8%e5%8d%95%e6%a0%b7%e5%bc%8f-pseudo-elements-style-form-controls/">伪元素表单控件默认样式重置与自定义大全</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[如何修改单选框、复选框、下拉框的默认样式]]></title>
        <id>https://zkrisj.gitee.io/post/ru-he-xiu-gai-dan-xuan-kuang-fu-xuan-kuang-xia-la-kuang-de-mo-ren-yang-shi/</id>
        <link href="https://zkrisj.gitee.io/post/ru-he-xiu-gai-dan-xuan-kuang-fu-xuan-kuang-xia-la-kuang-de-mo-ren-yang-shi/">
        </link>
        <updated>2022-12-05T16:24:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="介绍">介绍</h2>
<p>HTML 原生的单选框、复选框元素样式在各个浏览器上面由用户代理默认设置样式，如果在页面上应用了其他颜色或主题时，我们通常也相应的更改这些输入框或按钮的颜色或背景，否则会出现颜色与背景或主题不融入的样式不一致问题。</p>
<h2 id="appearance">appearance</h2>
<p>通常的做法是使用 display: none、绝对定位或隐藏它们，然后使用 :before 或 :after 伪元素实现样式。</p>
<p>从 Chrome 83 开始支持的 appearance CSS 属性提供了更有效的用于控制基于操作系统主题的 UI 控件的原生外观的方法。目前，除了 IE，其他浏览器都已支持，兼容性很好。</p>
<figure data-type="image" tabindex="1"><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/33913ab5a67c493d8f938d7b21a72411~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221205221051.png" loading="lazy"></figure>
<h2 id="单选框">单选框</h2>
<pre><code class="language-html">&lt;body&gt;
  &lt;label&gt;
    &lt;input type=&quot;radio&quot; name=&quot;contact&quot; value=&quot;email&quot; checked&gt;Email
  &lt;/label&gt;
  &lt;label&gt;
    &lt;input type=&quot;radio&quot; name=&quot;contact&quot; value=&quot;phone&quot;&gt;Phone
  &lt;/label&gt;
  &lt;label&gt;
    &lt;input type=&quot;radio&quot; name=&quot;contact&quot; value=&quot;mail&quot;&gt;Mail
  &lt;/label&gt;
&lt;/body&gt;
&lt;style&gt;
  input[type=radio] {
    appearance: none;
    border-radius: 50%;
    width: 16px;
    height: 16px;
    border: 2px solid #999;
    transition: 0.2s all linear;
    margin-right: 5px;
    vertical-align: top;
  }
  input[type=radio]:checked {
    border: 6px solid black;
  }
&lt;/style&gt;
</code></pre>
<iframe src="https://code.juejin.cn/pen/7173667078539313165"></iframe>
<h2 id="复选框">复选框</h2>
<pre><code class="language-html">&lt;body&gt;
  &lt;label class=&quot;label&quot;&gt;
    &lt;input type=&quot;checkbox&quot;&gt;
    &lt;i class=&quot;check&quot;&gt;&lt;/i&gt;
    &lt;span&gt;Apple&lt;/span&gt;
  &lt;/label&gt;
  &lt;label class=&quot;label&quot;&gt;
    &lt;input type=&quot;checkbox&quot;&gt;
    &lt;i class=&quot;check&quot;&gt;&lt;/i&gt;
    &lt;span&gt;Banana&lt;/span&gt;
  &lt;/label&gt;
  &lt;label class=&quot;label&quot;&gt;
    &lt;input type=&quot;checkbox&quot;&gt;
    &lt;i class=&quot;check&quot;&gt;&lt;/i&gt;
    &lt;span&gt;Pear&lt;/span&gt;
  &lt;/label&gt;
&lt;/body&gt;
&lt;style&gt;
  .label {
    display: inline-flex;
    align-items: center;
    margin: 1em .5em;
  }
  .label input[type=checkbox] {
    appearance: none;
  }
  .label input[type=checkbox]+.check {
    width: .5em;
    height: .5em;
    margin-right: .5em;
    border: solid black;
    transition: .2s;
  }
  .label input[type=checkbox]:checked+.check {
    height: .2em;
    border-top: transparent;
    border-right: transparent;
    transform: rotate(-45deg);
  }
&lt;/style&gt;
</code></pre>
<iframe src="https://code.juejin.cn/pen/7173670223390703652"></iframe>
<h2 id="下拉框">下拉框</h2>
<p><code>&lt;select&gt;</code> 元素的内部结构复杂，难以控制，所以很难用 CSS 进行高效的设计，但可以像其他元素一样改变某些方面——例如，调整 盒模型、显示的字体等，还可以使用 appearance 属性来去除默认的系统外观。如果要实现自定义 <code>&lt;select&gt;</code> 元素，可以考虑使用第三方 UI 库，或者尝试使用非语义元素和 JavaScript 来制作下拉菜单，再使用 WAI-ARIA 来提供语义。</p>
<iframe src="https://code.juejin.cn/pen/7173698045593255947"></iframe>
<h2 id="accent-color">accent-color</h2>
<figure data-type="image" tabindex="2"><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/bc5422aaa5c543a3bde553321522babe~tplv-k3u1fbpfcp-watermark.image?" alt="微信截图_20221205231321.png" loading="lazy"></figure>
<p>从 Chrome 93 开始支持的 CSS accent-color 属性提供了更简单的修改表单控件的样式的方法，它可以在不改变浏览器默认表单组件基本样式的前提下重置组件的颜色。目前支持下面这些 HTML 控件元素：</p>
<ul>
<li><code>&lt;input type=&quot;checkbox&quot;&gt;</code></li>
<li><code>&lt;input type=&quot;radio&quot;&gt;</code></li>
<li><code>&lt;input type=&quot;range&quot;&gt;</code></li>
<li><code>&lt;progress&gt;</code></li>
</ul>
<p>并且，accent-color 属性具有继承性，只需要在对应表单控件元素的祖先元素上设置，响应的控件的颜色就会发生变化。</p>
<iframe src="https://code.juejin.cn/pen/7173698375756283935"></iframe>
<h2 id="参考资料">参考资料</h2>
<ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Learn/Forms/How_to_build_custom_form_controls">如何构建表单小工具</a></li>
</ul>
]]></content>
    </entry>
</feed>